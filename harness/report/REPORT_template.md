# REPORT — FR3 handover bake-off, P4 compare + decide

**This is a pipeline proof, not a result.** Every stage of both lanes ran end
to end once, with tiny numbers (76 scripted-demo episodes, 2000 fine-tune
steps, 1–2 h of SONIC RL on one GPU, 20 evaluation rollouts). The table
below compares pipelines; it does not say which control stack is better, and
nothing in it should be read that way.

Generated by `python3 harness/report/aggregate.py` on {{GENERATED}} from the
run folders in section 2. Re-run the script to regenerate; the prose lives in
`harness/report/REPORT_template.md`.

## 1. Headline

- Same task (dual-FR3 block handover), same demo set, same VLA base
  (GR00T N1.7 3B), same fine-tune command, same evaluation binding, same
  {{N_ROLLOUTS}} seeded rollouts (seed 0 + episode index, spawn ±6 cm /
  ±0.5 rad) for both policies. The two oracles replay the recorded spawns of
  export episodes 0–19 on the table-driven env variant instead of the seeded
  spawns; see section 4.
- **Lane A {{A_SUCC}}, lane B {{B_SUCC}}, A-oracle {{OA_SUCC}}, B-oracle
  {{OB_SUCC}}.** Both policies are a tie at zero. Mean rubric progress
  (milestones reached / 6): lane A {{A_PROGRESS}}, lane B {{B_PROGRESS}},
  A-oracle {{OA_PROGRESS}}, B-oracle {{OB_PROGRESS}}.
- **The comparison that carries information is A-oracle vs B-oracle.**
  Replaying the recorded joint targets succeeds on every episode; replaying
  the encoder-labelled SONIC tokens through the trained decoder never gets
  past milestone 1. Lane B's ceiling is ≈ 0, so its policy number says nothing
  about GR00T-over-tokens: the controller (token space + decoder trained in
  P2) lost, not the VLA.
- With n = {{N_ROLLOUTS}}, 0/{{N_ROLLOUTS}} is compatible with any true
  success rate up to {{CI_ZERO_UPPER}} (exact 95 % interval), and
  {{N_ROLLOUTS}}/{{N_ROLLOUTS}} with anything above {{CI_FULL_LOWER}}. The
  milestone-1 rates (lane A {{A_M1}}, lane B {{B_M1}}, B-oracle {{OB_M1}})
  differ by one episode and are noise.

## 2. Sources

Newest run folder per category under `~/runs/franka-sonic` or the
instance-local fallback `/tmp/franka-sonic` (AGENTS.md rule k). Instance-local
folders do not survive a pod restart: {{INSTANCE_LOCAL}} — their
`eval_results.csv` and episode JSONs are small and are tagged `NEEDS-COPY` in
`plan/STATUS.md`.

{{SOURCES_TABLE}}

## 3. Results — the same 20 rollouts, four rows

{{MAIN_TABLE}}

Column notes:

- **full-success rate**: block at rest in the END zone with both grippers open
  (`handover_success`), evaluation terminates on confirmed success.
- **95 % CI**: exact (Clopper–Pearson) binomial interval on the success count.
- **milestone reach rates**: `handover_rubric()` in the franka repo, six
  milestones ordered by dependency (left reaches → left lifts → placed at
  centre → right reaches → right lifts → placed at end); milestone k counts
  as reached when `criteria_reached ≥ k` in `episode_<k>_result.json`.
- **mean progress**: the evaluator's own `progress` = milestones reached / 6,
  averaged over episodes.
- **median steps-to-success**: episode length of the successful episodes
  (terminate-on-success, so the length is the step at which the stable
  placement was confirmed); `n/a` when no episode succeeded.
- **action jerk**: not recorded by any of the four runs. `evaluation.eval`'s
  `--save-trace` accepts only the Cosmos client and was off; the ZmqAct policy
  servers log replan latency and, for lane B, the token magnitude, not the
  commanded targets; the oracle clients log nothing per step. Recording it
  needs the 16-D commanded target per 50 Hz step in the run folder (server
  side, or the trace extended to ZmqAct/oracle clients) and a third finite
  difference over it. Nothing in the run folders lets it be reconstructed, so
  it is left blank rather than estimated.
- **eval wall-clock**: run-folder span from the launch stamp to the bakeoff
  finalisation stamp; it includes model load (~2 min for a GR00T server) and
  per-episode video encoding. Every unsuccessful episode ran the full
  1500-step horizon (30 s of simulated time).
- **GPU-hours**: policy rows carry the lane's total over every run folder of
  that lane (RL, export, replay, labelling, fine-tune, open-loop, eval,
  oracle); oracle rows carry that oracle run alone. Derived from the run
  folders' stamps and device lists, appendix A. The shared demo pipeline
  (MimicGen generation, export, dataset conversion) is common to both lanes:
  {{GPU_TOTAL_SHARED}} GPU-hours, appendix A.

Per-milestone view of the same numbers:

{{MILESTONE_TABLE}}

Reading it: the A-oracle passes every milestone on every episode (steps to
success {{OA_MIN_STEPS}}–{{OA_MAX_STEPS}}, median {{OA_MEDIAN_STEPS}}). The
three other rows reach the block with the left arm in a few episodes and lift
it at most once (lane A, one episode reaching 3/6). Neither policy nor the
B-oracle ever hands the block over.

## 4. Fine-tune and evaluation parity

Both lanes ran `gr00t.experiment.launch_finetune` on the same base model
(`nvidia/GR00T-N1.7-3B`), the same embodiment tag (`NEW_EMBODIMENT`, fresh
action head), the same 76-episode / 51 027-frame dataset (byte-identical
frames, state, video and language; lane B swaps the 16-D joint action table
for a 66-D token + grips table), 2000 steps, global batch 32, 2 GPUs
(torchrun, 2 ranks, devices 5 and 7), save every 500 steps, the SONIC-tutorial
colour jitter, 4 dataloader workers, default LR/optimizer, no LoRA, action
horizon 40 at 50 Hz, ABSOLUTE / NON_EEF for every action group. Both were
served with a 40-step chunk replanned every 20 steps (2.5 Hz) at 50 Hz.

Arguments that differ between the two commands: {{PARITY_DIFFS}}.
{{PARITY_VERDICT}}

{{PARITY_TABLE}}

Lane A (`{{FT_A_DIR}}/cmd.sh`):

```
{{FT_A_CMD}}
```

Lane B (`{{FT_B_DIR}}/cmd.sh`):

```
{{FT_B_CMD}}
```

Modality configs (parsed from the files in the repo):

{{MODALITY_TABLE}}

Action encodings: lane A `joint_pos_l 7 | joint_pos_r 7 | grip 2` — absolute
joint targets (the recorded, joint-limit-clipped differential-IK targets) plus
absolute 0/1 gripper commands. Lane B `motion_token 64 | left_grip 1 |
right_grip 1` — the 64-D FSQ token of the P2 encoder (dequantised, on the
1/16 grid) plus the same gripper columns copied across.

Training loss is not comparable across lanes (0.129 vs 0.635 at step 2000:
different action spaces). Lane A's open-loop MAE fell monotonically 0.127 →
0.096 → 0.081 → 0.060 rad over checkpoints 500 → 2000, so the fine-tune did
learn the data; no open-loop eval was run for lane B.

Evaluation arguments, read from each run folder's `out/eval/config.json`:

{{EVAL_PARITY_TABLE}}

{{EVAL_PARITY_VERDICT}} The oracles run on the `OracleA-v0` env variant, which
places the cube at each export episode's recorded spawn instead of the seeded
random spawn, and their in-process clients take the grip command from the
dataset's binary labels, so the grip threshold shown for them is not used.
The task, rubric, rate, replan period, horizon and success criterion are the
same for all four rows.

## 5. Oracles — what the two calibration rows say

- **A-oracle {{OA_SUCC}}** is the sanity floor and it holds: the export
  episodes' `ik_target_clamped` joint labels and binary grip labels, replayed
  on the recorded cube spawns through the same evaluator, reproduce the
  handover every time. The dataset, the joint-position env, the wire
  contract and the rubric are sound, so lane A's policy number is meaningful.
- **B-oracle {{OB_SUCC}}** is lane B's ceiling, and it is ≈ 0. The same
  episodes' encoder tokens, streamed through the decoder at 50 Hz with no VLA
  in the loop, never lift the block. This was predicted by P2's decoder replay
  (mean joint error 0.663 rad on a demo clip against the < 0.1 rad target,
  gate P2 passed with that warning) and confirmed in P3 (teacher-forced
  replay 0.77 rad; left joint 7 wrapped by ≈ 3 rad, joints 1/3/5 of the left
  arm and 6/7 of the right ≈ 1 rad off, elbows fine, hand positions within
  ≈ 5–6 cm). The SONIC rewards are body-position/orientation kernels tuned
  for a humanoid; with no joint-space term a 7-DoF arm's null space is free,
  and the tracked link origins lie on the joint-1/joint-7 axes. The hands go
  roughly where they should with the wrist rotated, and the grasp misses.
- **Consequently lane B's {{B_SUCC}} is uninformative about the VLA.** Lane
  B's fine-tune emitted in-range tokens (|t| ≤ 1.0, none clipped) at the same
  latency as lane A; whether GR00T learned the token stream cannot be judged
  behind a decoder that cannot execute the labels. A B-oracle near the
  A-oracle is a precondition for reading lane B's row at all, and it belongs
  in the P2 gate.
- **Lane A's {{A_SUCC}} against a 20/20 oracle** is a policy/label problem,
  diagnosed in P1: open loop the policy tracks the demos to 0.06 rad MAE
  except in the first chunk, where it reproduces the labels' saturated
  differential-IK commands (2–4 rad swings on joints 1/3/6/7 at the arm's
  first move); closed loop those targets throw the arm into a pose it never
  recovers from. The label was chosen because it is the only one that
  replays the demos (next-state and delta-clamped labels failed the oracle).
  The two candidate fixes (an inference-side per-replan excursion clamp, or
  retraining on next-state labels with a slower oracle expectation) were
  recorded and not run.

## 6. Deviations and decisions taken in P0–P3

Every item is in `plan/STATUS.md` with its timestamp; this is the list a
reader needs to weigh the table.

**Pod and infrastructure**

- Only 2 of the 8 GPUs were allocatable (devices 5 and 7; the others hold
  foreign memory). PLAN.md's parallel schedule (A fine-tune 2 ‖ SONIC RL 4 ‖
  eval 1–2) became sequential: SONIC RL on one GPU with 2048 envs, fine-tunes
  on 2 GPUs, evals with the policy server and Isaac on the same device
  (replan latency ≈ 85–90 ms against a 400 ms replan period).
- No CUDA toolkit on the pod: DeepSpeed needs a `CUDA_HOME` stub with a fake
  `nvcc` (`harness/env/cuda_home_stub`); nothing is compiled. NCCL's P2P and
  shared-memory transports fault between the two devices, so both fine-tunes
  ran over loopback sockets (`NCCL_P2P_DISABLE=1 NCCL_SHM_DISABLE=1`);
  measured step rate 0.76–0.77 steps/s, not the bottleneck.
- Kit's portable-mode dirs under `/isaac-sim/kit` had to be chowned (the
  authorised rule-j exception) before any Isaac env would render.
  `nvidia-smi` is absent; the allocator uses its own probe.
- Storage: Lustre home is 99 % full and drained ≈ 100 GB/h from other users.
  The floor was raised to 600 GB free at 03:33 UTC; from 07:20 UTC every new
  run folder (all of P3: token labels, lane B dataset, lane B fine-tune, lane
  B eval, B-oracle) landed on instance-local `/tmp/franka-sonic`, which does
  not survive a pod restart. The lane B checkpoint-2000 model files were
  copied to `~/runs/franka-sonic/lane_b/final/2026-09-03_finetune/` (6.5 GB);
  the rest is `NEEDS-COPY`. Each GR00T checkpoint is 34 GB (27 GB of it
  DeepSpeed optimizer state), so the prescribed `--save-steps 500` wrote
  136 GB per lane.

**Demo set (P0/P1)**

- No human teleop: 10 scripted MimicGen sources recorded at 50 Hz (the
  recorder's goto/grip/settle step budgets had to be scaled with the rate),
  80 generated episodes, of which 76 replay to success at 50 Hz with cameras;
  the 4 that do not are flagged and excluded everywhere downstream.
- MimicGen's keep-rate was not recorded: Isaac Lab prints its tally to a
  block-buffered stdout that Kit's hard exit never flushes. From the per-episode
  generation time it was roughly one accepted episode per 2–3 trials; the
  ≥ 50 % target is unverified.
- On the 0.20 m angled rig the start slot sits exactly at the centre pad's
  0.10 m zone radius, so MimicGen saw the "placed at centre" signal true at
  frame 0 and refused the subtask boundaries. A copy of the annotated file with that
  signal zeroed before the first grasp (`harness/data/fix_subtask_signals.py`)
  was generated from instead; no upstream edit.
- Lane A's arm label is `ik_target_clamped` (the recorder's differential-IK
  joint command clipped to the FR3 joint limits), measured on one episode
  against the alternatives: raw IK 6/6 milestones but commands up to ≈ 10 rad
  outside the limits, clipped 6/6 with a sane range, per-step move clipped to
  0.3 rad 4/6, next measured joint position 4/6. The A-oracle and the dataset
  converter share the label function.
- Dataset route HDF5 → GR00T v2 directly (`lerobot` is not on the pod, so
  NVIDIA's v3 → v2 script is unusable). GR00T stacks all camera views into
  one tensor after its shortest-edge resize, so the 424×240 wrist videos were
  rescaled to the top camera's 640×360 (one resolution for all views); the
  live servers rescale their frames the same way.
- Dataset column order `[Lq7 | Rq7 | Lg Rg]` versus the sim's wire order
  `[Lq7, Lg, Rq7, Rg]`: converted inside both policy servers.

**Lane B embodiment and controller (P2)**

- No SMPL-X body models on the pod (registration-walled; BONES-SEED's SMPL
  side is an unextracted tar on read-only `/data`), so the motion library is
  demo-only: the 76 demos + mirrors + 7 augmentations = 1216 clips. The token
  space therefore covers tabletop handover reaching only. GMR is registered
  for the dual FR3 as a dormant path (`harness/lane_b/gmr/`), one body-model
  download away, but GMR's save path also assumes a floating base.
- Isaac Sim 5.1's MJCF importer is not enabled by the headless Kit experience
  and, enabled by hand, wrote empty USD layers and hung; the Isaac side spawns
  a URDF generated from the same MJCF. The URDF importer turns joint
  `friction` into a PhysX friction coefficient (menagerie's 1.137 N·m glued
  the joints), so URDF dynamics are 0/0. Self-collision is off (convex hulls
  interpenetrate at the folded ready pose; inter-arm contact is not
  modelled).
- Joint-6 convention: SONIC's q6 = FR3 q6 − 2.5307 rad, because the demos
  drive the left joint 6 beyond π and the motion library's axis-angle round
  trip wraps by 2π. Action scale 0.25 rad per unit (SONIC's effort/stiffness
  formula would not reach the demos' excursions); PD gains 400/80 (joints
  1–4) and 200/20 (5–7), the franka repo's high-PD tier.
- SONIC's humanoid-only terms (feet, undesired contacts, foot position, push)
  nulled; anchor = the fixed base; tracked bodies = link2/link4/link7 of each
  arm; the teleop and SMPL encoders stay in the module with zero sample
  probability because the ONNX export enumerates all three. The first RL
  smoke created one online wandb run before the harness switched to offline.
- RL budget: the plain variant 3563 PPO iterations in 1.75 h (2048 envs, one
  GPU) plateaued at 0.68 rad joint error with 5 cm body error; a first
  joint-space-reward variant (`jp`) was stopped at ≈ 900 iterations because
  its kernel had no gradient at the starting error; the second (`jp2`, every
  SONIC kernel at default + a joint-position term of weight 2) trained 2301
  iterations in 1.1 h to 0.62 rad. `jp2` is the exported pair (decoder replay
  0.663 vs 0.724 rad), so the gate reported it. Gate P2 passed with that
  warning against the < 0.1 rad target; that is the finding, not a pass.
- `simulation_app.close()` never returns headless on this pod; probes and
  stages hard-exit, and the bakeoff wall-clock cap kills by recorded PID.

**Lane B labels, fine-tune, eval (P3)**

- Token labels come from the `jp2` encoder over gear_sonic's own motion
  library loader (30 → 50 fps resample, its finite-difference velocities),
  validated in the SONIC env: 812/812 identical tokens on a demo clip, proprio
  layout measured (the PolicyCfg class order, not the yaml's). The env adds
  ±0.05 noise to the anchor-orientation input even at eval; the labels are
  noise-free by construction. |token| ≤ 1.0 on every frame.
- B-oracle design (prompt left open): in-process client running the same
  `sonic_decoder.py` as the server, on the OracleA env variant with the
  recorded spawn and the dataset's binary grip labels; decoder targets
  clipped to the FR3 joint limits in server and oracle; no decoder warm-up on
  reset; the VLA's token clipped to |t| ≤ 1.25 but not snapped to the grid;
  a "hold" token self-encoded from a static ready pose (saturated at ±1 on
  many dims — a static clip is off-distribution for this encoder) as the
  no-chunk fallback. `onnxruntime` was installed into the sim user-site for
  it (it dragged a numpy 2.4.6 into that user-site; inert under
  `python.sh`, a hazard for bare Kit-python).
- Lane B eval and B-oracle ran concurrently, one device each. A first
  detached launch failed to acquire GPUs (fine-tune memory not yet released)
  and a foreground retry left one Isaac process orphaned; it was stopped by
  its recorded PID and both stages were resumed into the same folders.
- Lane B's server never clipped a token and replanned at ≈ 91 ms mean.

## 7. What breaks at scale — deltas for a real run

1. **Demos.** 76 scripted-source MimicGen episodes give success rates with a
   ≈ 15-point noise floor at n = 20 and one task instance. A real run needs
   ≥ 1000 generated episodes from ≥ 50 human/teleop sources, the MimicGen
   keep-rate captured (flush the tally to a file, not stdout), and a
   validation split held out of both the fine-tune and lane B's motion
   library.
2. **Lane A's labels.** `ik_target_clamped` replays but carries saturated
   first-chunk commands the policy copies. Either record demos with a
   bounded-excursion controller, retrain on next-state labels with a slower
   oracle expectation, or add the inference-side excursion clamp and measure
   it into its own run folder. This is the cheapest experiment left undone.
3. **Motion library.** The full BONES-SEED upper-body subset retargeted
   through GMR (needs the SMPL-X body models, a fixed-base-aware GMR save
   path, the GMR-pkl → SONIC-pkl converter) on top of the demos, ≈ 1k clips
   plus mirrors, so the token space covers more than the demos' own
   trajectories.
4. **SONIC RL.** 4096 envs × 8 GPUs for ≥ 24 h (NVIDIA's recipe) instead of
   2048 envs × 1 GPU × 1–2 h; a joint-space tracking term from iteration 0;
   body kernels rescaled from humanoid std (0.3 m / 0.4 rad) to arm scale;
   a fix for the left joint-7 wrap (track a body off the joint-7 axis, or
   restrict the range in the embodiment); self-collision on; and the decoder
   replay gate enforced at < 0.1 rad **plus a B-oracle ≥ A-oracle − noise as
   a hard gate before any lane B fine-tune**. A decoder that cannot execute
   its own labels answers lane B before the VLA is trained.
5. **Fine-tune budget.** 20 000 steps for both lanes (NVIDIA's default; the
   prototype ran 10 % of it), same seeds, checkpoints at 5000-step intervals
   or model-only saves (34 GB per full checkpoint means 1.4 TB per lane at
   `--save-steps 500`), and an open-loop eval for lane B as well.
6. **Evaluation.** ≥ 100 rollouts per row (≈ 400 per row to resolve a
   10-point difference near 50 % at 95 % confidence with 80 % power),
   several checkpoints per lane, the same seeds
   for every row, the commanded targets logged per step so jerk and
   excursion can be reported, and the policy server on its own device.
7. **Infrastructure.** 8 allocatable devices (foreign memory on 6 of 8 today),
   a CUDA toolkit, working NCCL P2P, and storage far above the 600 GB floor
   (home is 99 % full on a shared filesystem; every P3 artifact sits on
   non-persistent instance storage).
8. **Real cell.** Both lanes emit absolute joint targets at 50 Hz. Running
   either on the physical FR3s needs a joint-impedance controller on the
   robots (libfranka joint impedance or ros2_control), which the repo does
   not have today, plus the per-step excursion cap from
   `policy_convention.yaml` (0.60 rad) enforced on the robot side. Lane B additionally needs the decoder's 10-step proprio
   history reproduced exactly on hardware (gravity direction, base angular
   velocity = 0 for the fixed base, joint positions relative to the default
   pose, velocities, last action). Neither stack is closer to hardware than
   the other after this prototype.

## 8. Where things are

- Lane A eval: `{{A_DIR}}`; A-oracle: `{{OA_DIR}}` (Lustre, persistent).
- Lane B eval: `{{B_DIR}}`; B-oracle: `{{OB_DIR}}` (instance-local, not
  persistent; `NEEDS-COPY` in STATUS.md).
- Lane A checkpoint-2000: `~/runs/franka-sonic/lane_a/2026-09-03_finetune/out/checkpoints/checkpoint-2000`.
  Lane B checkpoint-2000 model files:
  `~/runs/franka-sonic/lane_b/final/2026-09-03_finetune/checkpoint-2000`
  (the full checkpoint with optimizer state is on `/tmp`).
- SONIC encoder/decoder ONNX (`jp2`): `~/runs/franka-sonic/lane_b/2026-09-03_export_onnx-3/out/`.
- Demo export and GR00T v2 dataset: `~/runs/franka-sonic/shared/2026-09-03_demos-2/out/export`,
  `~/runs/franka-sonic/shared/2026-09-03_dataset-2/out/gr00t_v2`.
- Code: `harness/lane_a/`, `harness/lane_b/`, `harness/data/`,
  `harness/report/` in this repo; upstream working trees carry uncommitted
  installs listed in the WORKLOG.

## Appendix A — GPU-hours per run folder

Start = earliest of the README launch stamp and the first timestamped command
header in `logs/run.log` (so resumed folders include their earlier attempts
while the device claim was held); end = the bakeoff finalisation stamp in
`config.json`; GPU-hours = span × number of devices. Folders not counted
because their stamp holds no plain device list or no `config.json`:
{{GPU_UNRECORDED}}.

Shared (demo pipeline, common to both lanes) — total {{GPU_TOTAL_SHARED}} GPU-h:

{{GPU_TABLE_SHARED}}

Lane A — total {{GPU_TOTAL_A}} GPU-h:

{{GPU_TABLE_A}}

Lane B — total {{GPU_TOTAL_B}} GPU-h:

{{GPU_TABLE_B}}
