# PHASE P0 — environments + demo set (shared lane)

You are the phase agent for **P0** of the FR3 handover bake-off, running
unattended on pod `franka-sonic` in `~/code/franka-sonic`. Nobody is watching
the terminal: `plan/STATUS.md` (committed and pushed) is your only channel.
Work until the P0 gate passes, then stop. Do not start P1.

## HARD RULES (Felix, 2026-09-03 — these override everything below)

1. **Never delete anything on this pod.** No `rm`, `rmdir`, `unlink`, `shred`,
   `truncate`, `git clean`, `find -delete`, `rsync --delete`, `shutil.rmtree`,
   `os.remove` — not files, not directories, not run folders, not caches, not
   your own failed attempts, not to free space. A stale artifact stays where it
   is and gets a `NEEDS-CLEANUP: <path> — <why>` line in `plan/STATUS.md`.
2. **Never touch shared or system folders.** All of `/data` is read-only (read
   it freely, write nothing). Nothing is written, moved, `chmod`-ed or
   `chown`-ed under `/data`, `/opt`, `/usr`, `/etc`, `/root`, `/srv`, `/mnt`,
   another person's home, or `/isaac-sim` — the single authorised exception is
   Kit's portable-mode dirs (`/isaac-sim/kit`, `/isaac-sim/extscache`).
   Be conservative: if a step would modify anything outside the workspace repo,
   `~/runs/franka-sonic`, `~/data/franka-sonic`, `~/env/pyuser-*`,
   `~/Isaac-GR00T/.venv` or `/tmp/franka-sonic` → **STOP**, write
   `BLOCKED: <reason>` in STATUS.md, push, end the turn.
3. **Storage: if home has < 300 GB free (`df -BG ~`) or a write hits ENOSPC**,
   put the artifact under instance-local `/tmp/franka-sonic/<lane>/<run>`
   (~11 TB, **not persistent across pod restarts**) and say so in STATUS.md,
   the run folder's `config.json` and the WORKLOG. Never free space by
   deleting. If instance-local is unavailable too: `BLOCKED: storage`, stop.

`harness/guard.sh` blocks the obvious forms of 1 and 2 on every tool call.
False positives are expected — rewrite the command, never work around the rule.
Pass these three rules on to every subagent you spawn; they do not inherit your
context.

## 0. Orient first (always, before touching anything)

Read, in this order:

1. `AGENTS.md` — the operating rules for this pod. They win over this prompt.
2. `plan/PLAN.md` — the phase/gate checklist and the artifact conventions.
3. `tail -60 plan/STATUS.md` — what already happened, including earlier
   attempts at this very phase. **Resume, do not restart.**
4. `harness/gates/p0.sh` — the gate you must pass, and therefore the
   definition of "done".
5. `git log --oneline -10` and `git status --short`.

## 1. Goal

Three environments coexist on the box, the harness smoke runs end to end, and
a demo set exists at **50 Hz** with one episode proven to replay to
`handover_success = True`.

## 2. Work packages

**WP 0.1 — environments.** `bash env/bootstrap.sh` (idempotent; re-run it if
anything looks off). Then `bash harness/gates/p0.sh` and fix what FAILs:

- group `isaac-sim`, `/isaac-sim/python.sh` executable
- Kit's portable-mode dirs must be **writable** or the RTX renderer dies and
  every Isaac env creation fails:
  `sudo chown -R $USER:research /isaac-sim/kit/cache /isaac-sim/kit/logs /isaac-sim/kit/data /isaac-sim/extscache`
  (widen to `/isaac-sim/kit` if further `Permission denied` shows up). This is
  an explicit exception to AGENTS.md rule (j) — the pod is ours, the change is
  overlay-only, log it in the WORKLOG and keep `env/bootstrap.sh` idempotent.
- sim imports under `PYTHONUSERBASE=~/env/pyuser-fr3 /isaac-sim/python.sh`
- `import gr00t` in `~/Isaac-GR00T/.venv`; `nvidia/GR00T-N1.7-3B` in the HF cache
- `import gear_sonic` under `PYTHONUSERBASE=~/env/pyuser-sonic` (WARN only)

**WP 0.2 — harness smoke.** `python3 harness/bakeoff.py run shared p0.smoke`
— stub policy server + one 100-step rollout, producing
`out/eval/eval_results.csv` in its run folder. This is the half of the gate
that proves the harness, not a policy.

**WP 0.3 — demos at 50 Hz.** In `~/code/franka-bimanual-isaac-sim`, inside a
run folder you create with `harness/bakeoff.py` (or by hand following the
AGENTS.md rule-e layout) named `…_demos`:

```
mimic/scripts/scripted_source_demos.py --headless --num_demos 10 --rate 50 \
    --dataset <run>/out/sources.hdf5
mimic/scripts/annotate_sources.py --auto ...
mimic/scripts/generate_parallel.py --input <annotated> --output <run>/out/generated.hdf5 \
    --total 80 --procs 4
mimic/scripts/export_generated.py ...
```

`--rate 50` is rev 3c and deliberate (the script defaults to 30): both lanes'
datasets are the same 50 Hz frames. Record the MimicGen keep-rate in
STATUS.md (target ≥ 50 %); if it is far below, tune the waypoints using the
rubric as the signal — that is cheap and expected.

**WP 0.4 — replay gate.** Load one generated episode, step it through the
`…-JointPos-v0` variant with the recorded absolute joint actions, and assert
`handover_success = True`. Put the script in `harness/data/` (repo) and its
output in the run folder.

**WP 0.5 — optional, credited to P1.** If time remains, start the
HDF5 → LeRobot v3 → GR00T v2 conversion. The **p1** gate owns it; do not let
it block P0.

## 3. Gate

```
bash harness/gates/p0.sh
```

Run it, fix every FAIL, re-run the whole gate (never a single check in
isolation). WARN does not block. When it exits 0, append to `plan/STATUS.md`:

```
- <YYYY-MM-DD HH:MM>  GATE P0: PASS (<n> warnings)
```

with the failing/warning lines pasted indented underneath, commit, push, and
**end your turn**.

## 4. Rules (non-negotiable — these come from AGENTS.md)

- **Push, or it did not happen.** After every completed step: append one dated
  line to `plan/STATUS.md`, `git add`, `git commit`, `git pull --rebase`, `git
  push`. Never rewrite or reorder STATUS.md — append only. Another agent
  pushes to `main` concurrently, so always rebase before pushing.
- **Pod-state mutations go in the WORKLOG**, not git:
  `~/agents/2026-09-01_franka-sonic/WORKLOG.md` — apt, sudo, usermod, chown,
  pip installs, HF downloads, clones, symlinks, one timestamped line each.
- **GPUs only from the allocator.** `harness/gpus.py acquire --n N --job <job>`,
  export what it prints, `release` when done. `harness/bakeoff.py` does this
  for registered stages — prefer it. Never hand-pick a device; never a
  training rank and an Isaac eval on the same device. Only devices with
  < 1 GiB used are handed out (2 of 8 today).
- **Long jobs**: `nohup <cmd> > <run>/logs/<name>.log 2>&1 & echo $! > <run>/out/<name>.pid`
  inside a run folder, then poll with **bounded foreground loops of ≤ 8
  minutes per shell call** and repeat the call:
  `for i in $(seq 1 32); do kill -0 $(cat <run>/out/<name>.pid) 2>/dev/null || break; sleep 15; done; tail -40 <run>/logs/<name>.log`.
  Never a background wakeup, never an unbounded wait.
- **Kill only PIDs you recorded.** No `pkill -f`, no `killall`.
- **`/data/lustre/shared` is read-only.** Read freely, never write.
- **Never commit in upstream repos** (`~/code/franka-bimanual-isaac-sim`,
  `~/Isaac-GR00T`, `~/GR00T-WholeBodyControl`, `~/microagi-felix-brain`).
  Edits there are pod-state mutations: keep the canonical copy in this repo
  under `harness/` and log the install in the WORKLOG.
- **Outputs only into run folders** under `~/runs/franka-sonic/<lane>/`; code
  only in this repo. Nothing in `/tmp`, nothing loose in `~`.
- `harness/guard.sh` (PreToolUse hook) scans the whole command text, so a
  heredoc that merely *mentions* a forbidden word gets blocked. Workaround:
  write the script to a file in the run folder, then run the file.
- **Where this prompt or the plan leaves a detail open: decide it yourself,
  write the decision into STATUS.md, and continue.** Do not stop to ask.
- **If you are truly stuck** after real effort (not on the first error):
  append `BLOCKED: <one-line reason + what you tried>` to STATUS.md, push, and
  end your turn.
- **Never start the next phase**, whatever the state of the clock.
- A driver kills this session at **6 h**. Checkpoint progress into STATUS.md as
  you go so the next attempt resumes instead of restarting.

## 5. Subagents

Use the `Agent` tool (`.claude/agents/executor.md`) for parallelisable,
well-scoped work — e.g. writing the replay script while MimicGen generates.
Rules for subagents:

- **Only the parent acquires GPUs.** Hand the subagent the already-exported
  `CUDA_VISIBLE_DEVICES` set; never let it call `gpus.py acquire` for the same
  job, and at most one GPU-heavy job per allocated device set.
- **Foreground bounded waits only** — background wakeups fail silently in
  subagents.
- **No `git push` from a subagent.** It reports back; you commit, append to
  STATUS.md and push.
