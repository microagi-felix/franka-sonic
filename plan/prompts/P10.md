# PHASE P10 — the real comparison (100 rollouts) and the report

You are the phase agent for **P10** of the FR3 handover bake-off, running
unattended on pod `franka-sonic` in `~/code/franka-sonic`. Nobody is watching
the terminal: `plan/STATUS.md` (committed and pushed) is your only channel.
Work until the P10 gate passes, then stop.

## HARD RULES (Felix, 2026-09-03 — these override everything below)

1. **Never delete anything on this pod.** No `rm`, `rmdir`, `unlink`, `shred`,
   `truncate`, `git clean`, `find -delete`, `rsync --delete`, `shutil.rmtree`,
   `os.remove` — not files, not directories, not run folders, not caches, not
   your own failed attempts, not to free space. A stale artifact stays where it
   is and gets a `NEEDS-CLEANUP: <path> — <why>` line in `plan/STATUS.md`.
   This includes trainer housekeeping: never let a fine-tune rotate checkpoints
   away (`--save-total-limit` is set high enough to keep every one).
2. **Never touch shared or system folders.** All of `/data` is read-only (read
   it freely, write nothing). Nothing is written, moved, `chmod`-ed or
   `chown`-ed under `/data`, `/opt`, `/usr`, `/etc`, `/root`, `/srv`, `/mnt`,
   another person's home, or `/isaac-sim` — the single authorised exception is
   Kit's portable-mode dirs (`/isaac-sim/kit`, `/isaac-sim/extscache`).
   Be conservative: if a step would modify anything outside the workspace repo,
   `~/runs/franka-sonic`, `~/data/franka-sonic`, `~/env/pyuser-*`,
   `~/Isaac-GR00T/.venv` or `/tmp/franka-sonic` → **STOP**, write
   `BLOCKED: <reason>` in STATUS.md, push, end the turn.
3. **Storage: the floor is 600 GB free (`df -BG ~`).** Below it, or on any
   ENOSPC, the artifact goes to instance-local `/tmp/franka-sonic/<lane>/<run>`
   (~11 TB, **not persistent across pod restarts**); `harness/bakeoff.py` routes
   new run folders there by itself and stamps `run_root_fallback` into
   `config.json` (`python3 harness/bakeoff.py root` shows which root you get).
   Record every `/tmp` run path in STATUS.md tagged `(instance-local, not
   persistent)`. At the end of the phase, if a final model checkpoint sits on
   `/tmp` and home has > 100 GB free, copy **only the final model directory**
   (bf16 weights + processor/config, never `global_step*` or optimizer state)
   to `~/runs/franka-sonic/<lane>/final/<run-name>/`; otherwise append
   `NEEDS-COPY: <path>` to STATUS.md. Never free space by deleting. If both
   roots are unavailable: `BLOCKED: storage`, stop.

`harness/guard.sh` blocks the obvious forms of 1 and 2 on every tool call.
False positives are expected — rewrite the command, never work around the rule.
Pass these three rules on to every subagent you spawn; they do not inherit your
context.

## Round 2 in one paragraph (read this first)

Round 1 (P0–P6) proved the pipeline and produced a decoder that works: lane A
0/20, lane B 1/20, A-oracle 20/20, B-oracle 19/20 on 76 scripted-demo episodes
and a 2000-step fine-tune. Both policies were starved — 76 episodes, ~1.25
epochs. Round 2 (P7–P10) gives **both lanes the same, much larger training set
and a much longer fine-tune with an explicit stopping rule**, then compares
them on 100 rollouts. Nothing about the comparison is allowed to differ between
the lanes except the action interface: same episodes, same fine-tune
hyper-parameters and step budget, same stopping rule, same evaluation binding,
seeds, rate, replan interval and horizon.

## 1. Goal

Run the comparison the whole project exists for: **100 rollouts per
row**, four rows (lane A, A-oracle, lane B, B-oracle), one identical evaluation,
then a report that states what the numbers do and do not support.

## 0. Orient first

1. `AGENTS.md`; 2. `tail -100 plan/STATUS.md` — the two `P9 BEST lane_x=…`
lines, P8's decoder export, P7's demo run; 3. `harness/gates/p10.sh`;
4. `plan/REPORT.md` and `harness/report/aggregate.py` + `REPORT_template.md`
(round 1's report is the shape to keep; it is already P5/P6-aware with exact
Clopper–Pearson intervals).

Round-1 measurements: 20 rollouts ≈ 22 min for a policy, ≈ 7 min for an oracle.
100 rollouts ≈ 1.8 h per policy, ≈ 35 min per oracle. Two policies in parallel
on separate device sets, then the two oracles in parallel: ~2.5 h total.

## 2. Work packages

### WP 10.1 — the two policies, 100 rollouts each
```
python3 harness/bakeoff.py run lane_a eval --checkpoint <P9 BEST lane_a> --rollouts 100
python3 harness/bakeoff.py run lane_b eval --checkpoint <P9 BEST lane_b> --onnx <P8 export> --rollouts 100
```
Identical binding, seeds (`seed 0 + episode index`, episodes 0–99), rate 50 Hz,
replan every 20, horizon 1500. Verify lane B's server log names the **P8**
decoder before the rollouts start.

### WP 10.2 — the two oracles, 100 rollouts each
`oracle_a` on the recorded joint targets and `oracle_b` on the P8 encoder's
tokens, both over export episodes 0–99 of the **P7** demo set. These two rows
are the ceilings the policy rows are read against.

### WP 10.3 — report
Regenerate `plan/REPORT.md` with `harness/report/aggregate.py`. The aggregator
picks the newest run per category — if the four runs you want are not the
newest, add explicit `--lane-a-eval/--lane-b-eval/--oracle-a/--oracle-b` path
flags rather than re-running anything to change the ordering.

The report must carry, beyond round 1's table: the P9 screening series for both
lanes (the learning curves), the checkpoint each lane's rule chose, exact 95 %
intervals at n = 100, whether the two policy intervals overlap, per-milestone
reach rates, and GPU-hours per lane with lane B's SONIC RL counted in.

**Write the verdict honestly.** Say which lane won, by how much, and whether
n = 100 separates them. Name the limits that remain: the demos are scripted, so
both policies inherit one style; one seed set; one task; one architecture pair;
the oracles bound each lane's interface, not its policy. If the two lanes are
statistically indistinguishable, say that — it is a result, not a failure.

### WP 10.4 — close the round
One STATUS line with the four rows and the verdict sentence. List the harness
debts still open (`bakeoff` finalizer must wait on the trainer python pid;
action jerk is still not recorded — the 16-D commanded target per 50 Hz step
would have to be logged server-side). Then stop: there is no P11.

## Gate

```
bash harness/gates/p10.sh
```

When it exits 0, append `- <YYYY-MM-DD HH:MM>  GATE P10: PASS (<n> warnings)`
to `plan/STATUS.md` with the numbers named above on indented lines, commit,
push, **end your turn**.

## Rules (non-negotiable — these come from AGENTS.md)

- **Push, or it did not happen.** After every completed step: append one dated
  line to `plan/STATUS.md`, `git add`, `git commit`, `git pull --rebase`, `git
  push`. Never rewrite or reorder STATUS.md — append only. Another agent
  pushes to `main` concurrently, so always rebase before pushing.
- **Pod-state mutations go in the WORKLOG**, not git:
  `~/agents/2026-09-01_franka-sonic/WORKLOG.md`.
- Kit's portable-mode dirs must stay writable or every Isaac env creation dies:
  `sudo chown -R $USER:research /isaac-sim/kit/cache /isaac-sim/kit/logs /isaac-sim/kit/data /isaac-sim/extscache`
  (widen to `/isaac-sim/kit` if further `Permission denied` appears). Explicit
  exception to AGENTS.md rule (j); log it in the WORKLOG.
- **GPUs only from the allocator.** `harness/gpus.py acquire --n N --job <job>`,
  export what it prints, `release` when done. Never hand-pick a device; never a
  training rank and an Isaac eval on the same device. The allocator hands out
  devices with < 40 GiB used (the 5–19 GiB of foreign memory on devices 0–4 and
  6 is idle and was measured harmless on 2026-09-03).
- **Job names must be unique across run roots.** `harness/gpus.py` keys claims
  by `<lane>-<stage>-<run folder>`; two runs of the same stage in different
  roots collided once. Include the full run-folder name.
- **Long jobs**: `nohup <cmd> > <run>/logs/<name>.log 2>&1 & echo $! > <run>/out/<name>.pid`,
  then poll with **bounded foreground loops of ≤ 8 minutes per shell call**,
  repeating the call. Never a background wakeup, never an unbounded wait.
- **Kill only PIDs you recorded.** No `pkill -f`, no `killall`. Isaac trainers
  leave the Kit python alive after a wall-clock cap (`close()` hangs): SIGTERM
  the recorded pgid, then SIGKILL the python by pid, and verify with `ps`.
- **`/data/lustre/shared` is read-only.**
- **Never commit in upstream repos.** Canonical copies live in this repo under
  `harness/lane_a/` and `harness/lane_b/`.
- **Outputs only into run folders** under `~/runs/franka-sonic/<lane>/`; code
  only in this repo. Installs and clones only under `~` (the named locations);
  run folders may live under `/tmp/franka-sonic` only when `harness/bakeoff.py`
  routes them there (instance-local, not persistent) — nothing else in `/tmp`,
  nothing loose in `~`.
- `harness/guard.sh` (PreToolUse hook) scans the whole command text, so a
  heredoc that merely *mentions* a forbidden word gets blocked. Workaround:
  write the script to a file in the run folder, then run the file.
- **Where this prompt or the plan leaves a detail open: decide it yourself,
  write the decision into STATUS.md, and continue.** Do not stop to ask.
- **If you are truly stuck** after real effort (not on the first error):
  append `BLOCKED: <one-line reason + what you tried>` to STATUS.md, push, and
  end your turn.
- **Never start the next phase**, whatever the state of the clock.
- A driver kills this session at **8 h**. Checkpoint progress into STATUS.md as
  you go so the next attempt resumes instead of restarting.

## Subagents

Use the `Agent` tool (`.claude/agents/executor.md`) for parallelisable work.

- **Only the parent acquires GPUs.** Hand the subagent the already-exported
  `CUDA_VISIBLE_DEVICES` set; never let it call `gpus.py acquire` for the same
  job, and at most one GPU-heavy job per allocated device set.
- **Foreground bounded waits only** — background wakeups fail silently in
  subagents.
- **No `git push` from a subagent.** It reports back; you commit, append to
  STATUS.md and push.
