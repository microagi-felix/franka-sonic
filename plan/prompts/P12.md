# PHASE P12 — round 3b: settle what round 3 left open (concurrency, lane A's budget, the bistable regime)

You are the phase agent for **P12** of the FR3 handover bake-off, running
unattended on pod `franka-sonic` in `~/code/franka-sonic`. Nobody is watching
the terminal: `plan/STATUS.md` (committed and pushed) is your only channel, and
`plan/ORCHESTRATOR_NOTES.md` (echoed by every `harness/bakeoff.py` call, newest
entry first) is the orchestrator's channel to you — act on it. Also print
`tail -3 /tmp/franka-sonic/p12/notes.log` at the top of every poll you write;
the orchestrator appends urgent lines there. Work until the P12 gate passes,
then stop. P11's "there is no P12" is superseded by this prompt.

## HARD RULES (Felix, 2026-09-03 — these override everything below)

1. **Never delete anything on this pod.** No `rm`, `rmdir`, `unlink`, `shred`,
   `truncate`, `git clean`, `find -delete`, `rsync --delete`, `shutil.rmtree`,
   `os.remove` — not files, not directories, not run folders, not caches, not
   your own failed attempts, not to free space. A stale artifact stays where it
   is and gets a `NEEDS-CLEANUP: <path> — <why>` line in `plan/STATUS.md`.
   This includes trainer housekeeping: never let a fine-tune rotate checkpoints
   away (`--save-total-limit` is set high enough to keep every one).
2. **Never touch shared or system folders.** All of `/data` is read-only (read
   it freely, write nothing). Nothing is written, moved, `chmod`-ed or
   `chown`-ed under `/data`, `/opt`, `/usr`, `/etc`, `/root`, `/srv`, `/mnt`,
   another person's home, or `/isaac-sim` — the single authorised exception is
   Kit's portable-mode dirs (`/isaac-sim/kit`, `/isaac-sim/extscache`).
   Be conservative: if a step would modify anything outside the workspace repo,
   `~/runs/franka-sonic`, `~/data/franka-sonic`, `~/env/pyuser-*`,
   `~/Isaac-GR00T/.venv` or `/tmp/franka-sonic` → **STOP**, write
   `BLOCKED: <reason>` in STATUS.md, push, end the turn.
3. **Storage: the floor is 600 GB free (`df -BG ~`).** Below it, or on any
   ENOSPC, the artifact goes to instance-local `/tmp/franka-sonic/<lane>/<run>`
   (~11 TB, **not persistent across pod restarts**). Fine-tunes and rows in
   P12 go to `/tmp` from step 0 (`BAKEOFF_RUN_ROOT=/tmp/franka-sonic`), as in
   P11; record every `/tmp` run path in STATUS.md tagged `(instance-local,
   not persistent)`. Model directories that the report quotes are copied to
   `~/runs/franka-sonic/<lane>/final/p12/<checkpoint>` (bf16 weights +
   processor / config / statistics, never `global_step*`, optimizer or rng
   state) — WP 12.0 and WP 12.6 say which. Never free space by deleting.

`harness/guard.sh` blocks the obvious forms of 1 and 2 on every tool call.
False positives are expected — rewrite the command, never work around the rule.
Pass these three rules on to every subagent you spawn; they do not inherit your
context.

## Round 3b in one paragraph (read this first)

P11 closed with GATE P11 PASS (2026-09-06 02:10 UTC). On the live episodes of
the held-out slice 20–199, lane A's round-3 `checkpoint-20000` scored 114/159
= 72 % and lane B's round-3 `checkpoint-10000` 177/180 = 98 %; both beat their
own round-2 checkpoints re-measured concurrently (37 % and 46 %). Three things
keep that verdict from being clean, and P12 exists to settle them. (1) **The
two headline rows were not concurrent**: lane B's was unseeded and ran
16:15–18:24 under a light load, lane A's was seeded and ran 21:05–00:08 under
six to ten concurrent evaluations. (2) **Lane A may be under-trained at the
shared budget**: its round-3 screens were 0/2/0/0/0/1/5/13 — still climbing
steeply at the last checkpoint — while lane B plateaued by mid-budget. (3) **A
lane-B checkpoint's run can flip mid-run between a task-completing regime and a
stalling one** (checkpoint-17500: 3/81, 100/106, 1/12 in one 200-rollout row;
another run flipped the other way at episode 12; the launch path, seed,
checkpoint files, device, environment, ONNX provider, ports, code drift,
server latency, rendering, image lag, camera alignment, decoder state and the
client loop are all excluded with data — see `plan/ORCHESTRATOR_NOTES.md`
22:50 → 01:25 entries of 2026-09-05/06). P12 runs (1) a concurrent, seeded,
load-matched re-measurement of both headline checkpoints, (2) a lane-A budget
extension under the identical recipe, and (3) a determinism probe of the
lane-B policy function. Nothing else changes: same binding, same seeds, same
rate, same replan interval, same horizon, same rubric, same selection rule.

## 0. Orient first

1. `AGENTS.md`; 2. `tail -200 plan/STATUS.md` — P11's rows, the 02:10 closing
entry, the harness debts (a–j); 3. `plan/ORCHESTRATOR_NOTES.md` entries from
22:50 (2026-09-05) to 01:25 (2026-09-06) — the reporting rules there apply to
every number in P12: rates on live episodes of 20–199, dead counts beside
them, regime-flipping rows segmented at their flips, no interval on a mixture;
4. `plan/REPORT.md` sections 10.3a–10.3c and 10.6; 5. `harness/gates/p12.sh`;
6. `harness/report/aggregate.py` (`--row`, `--r3-*`, `--held-out-from`).

Fixed paths (verify each exists before you use it; `/tmp` paths vanish on a
pod restart — check first, and say in STATUS which ones you found):

```
UDS   ~/runs/franka-sonic/shared/2026-09-05_dataset/out/gr00t_v2                 union lane-A dataset, 1855 episodes (WP 11.1)
UTOK  ~/runs/franka-sonic/lane_b/2026-09-05_label_tokens                         union tokens + out/gr00t_v2_sonic, 1855 episodes (WP 11.3)
ONNX  ~/runs/franka-sonic/lane_b/2026-09-04_export_onnx-19                       decoder/encoder pair — unchanged
R2    ~/runs/franka-sonic/shared/2026-09-04_demos                                wide-spawn demos (rows pass --demos explicitly, as P11 did)
R3    ~/runs/franka-sonic/shared/2026-09-04_demos-2                              eval-matched demos
CKA3  /tmp/franka-sonic/lane_a/2026-09-05_finetune-2/out/checkpoints/checkpoint-20000   lane A round-3 winner (instance-local)
CKA3h ~/runs/franka-sonic/lane_a/final/p11/checkpoint-20000                     its rescued bf16 copy — same weights + processor; use it if CKA3 is gone
CKB3  /tmp/franka-sonic/lane_b/2026-09-05_finetune-2/out/checkpoints/checkpoint-10000   lane B round-3 quoted row (instance-local, NOT yet rescued)
CKB3p /tmp/franka-sonic/lane_b/2026-09-05_finetune-2/out/checkpoints/checkpoint-17500   lane B pre-registered pick (regime-flipping); rescued copy ~/runs/franka-sonic/lane_b/final/p11/checkpoint-17500
ROWA  /tmp/franka-sonic/lane_a/2026-09-05_eval-9/cmd.sh                          the exact lane-A 200-row command of round 3 (seeded)
ROWB  /tmp/franka-sonic/lane_b/2026-09-05_eval-24/cmd.sh                         the exact lane-B 200-row command of round 3 (seeded)
```

If `ROWA`/`ROWB` are gone, the round-3 row command is reconstructed from
`harness/bakeoff.py run <lane> eval --gpus 1 --checkpoint <ck> --rollouts 200
--server-seed <seed>` with the explicit `--demos $R2`, and for lane B `--onnx
$ONNX --tokens $UTOK`; compare your reconstruction against
`plan/REPORT.md` section 13 ("Where things are") before the first launch.

Round-3 timings on this pod: fine-tune ~1.7 s/it on 2 GPUs (9.5 h for 20 000
steps), a 20-rollout screen ~25 min, a seeded 200-rollout row 3–4.5 h (lane B
in its completing regime ~2.5 h), the evaluation client ~40 s of start-up.

## 1. Goal

Four concurrent seeded rows (two per lane, two server seeds each) of the two
round-3 headline checkpoints; lane A trained 20 000 further steps under the
identical recipe, every checkpoint screened twice, the best and the last at 200
rollouts; a determinism probe that says whether the lane-B policy function is
bit-reproducible within a process, across processes, and under load; a report
section that puts all of it beside round 3 with the same verdict rules.

## 2. Work packages

### WP 12.0 — rescue first (ten minutes, before anything else)

If `CKB3` exists, copy its model directory to
`~/runs/franka-sonic/lane_b/final/p11/checkpoint-10000` exactly as WP 11.8 did
for the winners (bf16 weights + processor + config + statistics; not
`global_step*`, not optimizer, not `rng_state_*`); byte-size-verify every file
against its source and write one STATUS line `P12 RESCUE lane_b=<abs path of the copy>`.
It is the row the report quotes as lane B's round-3 result and it exists nowhere
persistent. If `CKB3` is gone (pod restart), write `P12 RESCUE lane_b=MISSING`
and follow the fallback in WP 12.1.

### WP 12.1 — the concurrent seeded re-measurement (start immediately after 12.0)

Four rows, launched within five minutes of each other, one device each from the
allocator, all on `/tmp` (`BAKEOFF_RUN_ROOT=/tmp/franka-sonic`), each the
verbatim round-3 row command (`ROWA` / `ROWB`) with only the checkpoint and the
seed changed:

| label | checkpoint | `--server-seed` |
|---|---|---|
| `A_R3_20000_s05` | CKA3 (or CKA3h) | 20260905 |
| `A_R3_20000_s09` | CKA3 (or CKA3h) | 20260909 |
| `B_R3_10000_s05` | CKB3 (or the WP 12.0 copy) | 20260905 |
| `B_R3_10000_s09` | CKB3 (or the WP 12.0 copy) | 20260909 |

Record the launch time, device, port and launcher pid of each in STATUS as one
`P12 ROW <label>=<abs run folder>` line per row (absolute paths, nothing else
on the line). Nothing else runs on the node while the first 20 episodes of
each row are in flight except WP 12.2's fine-tune (start it right after the
four rows are up); no screens, no probes, until all four rows are past
episode 20. Report every row exactly as P11's 10.3c does: per-episode outcome
string (`1` success, `z` dead, `0` other), dead count, all-200 and live
held-out 20–199 with the exact 95 % interval on the live rate, and a regime
segmentation (window 8, threshold 0.5) for any row whose outcome string has a
run of ≥ 12 consecutive live failures or ≥ 12 consecutive successes adjacent
to a run of the opposite kind. The two seeds of one checkpoint give the
sampler's spread; the two lanes at one time give the load-matched comparison.

**Fallback if CKB3 is missing and no copy exists**: run the two lane-B rows on
`CKB3p` (the rescued `checkpoint-17500`) instead, label them
`B_R3_17500_s05/s09`, and write `NEEDS-DECISION: lane B's quoted round-3 row
(checkpoint-10000) is not recoverable; the concurrent comparison uses the
pre-registered checkpoint-17500 instead — retrain lane B's round-3 recipe to
recover a checkpoint-10000?` in STATUS. Do not start a lane-B fine-tune on
your own.

### WP 12.2 — lane A's budget extension (start right after the four rows are up)

```
BAKEOFF_RUN_ROOT=/tmp/franka-sonic python3 harness/bakeoff.py run lane_a finetune --gpus 2 \
    --dataset ~/runs/franka-sonic/shared/2026-09-05_dataset --init-from <CKA3 or CKA3h> \
    --train-steps 20000 --save-steps 2500 --save-total-limit 12
```

Token for token the round-3 command (`/tmp/franka-sonic/lane_a/2026-09-05_finetune-2/cmd.sh`
if it still exists; otherwise WP 11.4 of `plan/prompts/P11.md`) with only
`--init-from` changed: the same union data, the same hyper-parameters, a fresh
optimizer and cosine schedule — i.e. the recipe is now "60k steps with two
restarts", and the report says so. Verify the warm start within the first ten
minutes exactly as WP 11.4 did and write the line `P12 INIT lane_a=<abs
checkpoint path>` followed by the indented evidence (loading summary, mean loss
over steps 10–100 near round 3's end, whether `statistics.json` is inherited or
re-derived). Write `out/finetune.pid` / `.pgid`. If STATUS already carries a
`P12 INIT lane_a=` line from a previous attempt, `ps -p <pid>` first and adopt
the run — never launch a second one.

Screens: every `checkpoint-2500 … 20000` at 20 rollouts **twice** — two
independent launches per checkpoint, at least ten minutes apart, one device
each — as each checkpoint settles. Two screens per checkpoint is the P11
lesson (a single screen measured a regime, not a checkpoint). Reuse
`/tmp/franka-sonic/p11/watcher.py` (already fixed: run folder from the
launcher log, zombie reap) or launch by hand from bounded polls; either way the
screen's run folder comes from the launcher's own `[bakeoff] OK rc=0  log:
<run>/logs/run.log` line, never from a directory listing. Ranking rule,
pre-registered and unchanged: **(successes summed over both screens,
milestone-6 rate, milestone-5 rate, step)** — never mean progress. Write:

```
P12 BEST lane_a=<abs path>
```

(the gate takes the *last* match; never re-quote a template after the real
line). Then two 200-rollout rows, seeded `20260905`, the verbatim `ROWA`
command with the checkpoint changed: the best checkpoint and `checkpoint-20000`
(if they coincide, the second row is the runner-up). `P12 ROW A_EXT_<step>_s05=<abs run folder>` lines as above.

Also write, in STATUS, the training-loss curve of the extension beside round
3's (`trainer_state.json` of both runs; mean over each 2500-step window) so the
report can say whether the loss was still falling when the cosine hit zero.

### WP 12.3 — the determinism probe of the lane-B policy function

Purpose: is the per-process bistable regime in the policy function (GR00T
forward + decoder) or outside it (simulator, observation pipeline)? The probe
answers the first half bitwise.

1. Add an opt-in `--dump-requests <dir> --dump-n N` to
   `harness/lane_a/serve_gr00t_joint.py` (lane B's server inherits it): the
   first N `act` requests of the process (state16 and the three images,
   verbatim, plus the reply row and, for lane B, the 40×66 chunk at each
   replan) are saved as `request_<k>.npz`. Off by default; no other behaviour
   changes; `python -m py_compile` both servers.
2. Capture: one 20-rollout screen of `CKB3p` (checkpoint-17500 — the
   regime-flipping one) with `--dump-requests` and `--dump-n 400` (about five
   episodes' worth), on `/tmp`, the round-3 binding, unseeded. Its outcome
   string goes to STATUS like any screen.
3. `harness/lane_b/probe_determinism.py`: loads the policy exactly as the
   server does (same modality config, same checkpoint, same image resize),
   seeds exactly as `--server-seed 20260905` would for episode 0, and for each
   dumped request runs the forward **K = 5 times** in one process, recording the
   chunk each time. Reports, per request: max |Δ| between repeats within the
   process (bitwise-equal or not), and the fraction of the 64 token values that
   land on the 1/16 quantisation grid. Then the same script in **three fresh
   processes** (sequential, one device), and the same in **two processes while
   a 20-rollout screen of any checkpoint runs on the same device** ("under
   load"). Compare across processes: bitwise-equal, or max |Δ| and which
   requests differ. One JSON + one markdown summary in the probe's run folder
   under `/tmp/franka-sonic/lane_b/<date>_probe`, and one STATUS line
   `P12 PROBE=<abs path of the markdown>` whose file contains a line starting
   with `VERDICT:` — one of `VERDICT: bitwise-reproducible within and across
   processes (quiet and under load)`, `VERDICT: differs across processes only
   under load (max |dtoken| = …)`, `VERDICT: differs across processes even
   when quiet (…)`, or `VERDICT: differs within a process (…)`.
4. Only if the verdict is "bitwise-reproducible": the regime is outside the
   policy function, and you write that conclusion into the report with the
   list of what remains (PhysX state across resets, observation pipeline
   timing). If it differs: quantify against the 1/16 grid and say whether a
   difference of that size could move a decoder step (`harness/lane_b/
   sonic_decoder.py` clips at 1.25 and decodes continuous values — a Δ below
   1/32 is a sub-grid perturbation; report it, do not interpret further).

**Optional, only after WP 12.1–12.3 are complete and reported (WP 12.3b):**
the P11 observation that both good→bad flips coincided with a CPU transient.
During one 20-rollout screen of `CKB3p` that has produced ≥ 4 consecutive
successes, start a 90-second CPU burst — 96 single-threaded busy loops
launched by one script that records every pid to a file and kills them by pid
after 90 s (`kill` on recorded pids only; verify with `ps`) — and report the
screen's outcome string with the burst episode marked. One trial. Nothing else
is running that could be disturbed (no rows in their first 20 episodes, no
fine-tune within ten minutes of a checkpoint save). If any of those hold, skip
it and say so.

### WP 12.4 — wide-spawn rows (optional; only if the harness already supports it)

`harness/lane_b/eval_oracle_b.py` and the A-oracle replay the recorded spawns of
export episodes through a table-driven env variant. If `evaluation.eval` (the
upstream client) accepts that same spawn table for a *policy* client without a
code change beyond a flag, run both headline checkpoints on the first 200
wide-demo spawns of `$R2` (200 rollouts each, seeded 20260905, one device each,
after WP 12.1's rows are done) and report them as `A_R3_20000_wide` /
`B_R3_10000_wide` with the P11 rules. If it needs a patch, do not write one:
record `SKIPPED WP 12.4: <what would be needed>` in STATUS.

### WP 12.5 — report

Extend `plan/REPORT.md` (regenerate with `harness/report/aggregate.py` — add
`--r3b-row <label>=<run folder>` inputs, never reorder existing runs) with a
**Round 3b** section (number it 10.11 onward, or 14 if `aggregate.py` prefers a
new top-level section; keep every existing section byte-identical):
(a) the four concurrent rows in one table with round 3's headline rows beside
them, live held-out rates with intervals, dead counts, outcome strings,
regime segments where present; (b) the sampler spread (two seeds per
checkpoint) and the load-matched lane gap with its interval; (c) lane A's
extension — the screen series (both screens per checkpoint), the loss curve
beside round 3's, the two rows, and the sentence "lane A at 60k is X vs 72 %
at 40k"; (d) the probe verdict verbatim with its numbers; (e) WP 12.3b/12.4 if
run; (f) what round 3b does not support, and the harness debts a–j carried
forward plus anything new. Every verdict on the live held-out slice; no
interval on a mixture; a regime-flipping row is segmented, never averaged.

### WP 12.6 — close the round

HARD RULE 3 copies to `final/p12/`: the lane-A extension's best and last
checkpoints (model dirs only), and — if WP 12.0 could not run — nothing for
lane B. One STATUS line with the rows, the extension verdict, the probe
verdict, and the harness debts. Then stop: **there is no P13.**

## Gate

```
bash harness/gates/p12.sh
```

(defaults: ≥ 4 seeded post-epoch rows at ≥ 200 rollouts, ≥ 2 per lane, of
checkpoints that are either round-3 headline checkpoints or post-epoch; `P12
INIT lane_a=` naming a directory with `config.json`; ≥ 8 lane-A checkpoint
dirs newer than the epoch; ≥ 16 screens of post-epoch lane-A checkpoints at
≥ 20 rollouts; ≥ 1 row at ≥ 200 of a post-epoch lane-A checkpoint; `P12 BEST
lane_a=` naming a post-epoch directory; `P12 PROBE=` naming a markdown with a
`VERDICT:` line; `P12 RESCUE lane_b=` present (a path or `MISSING`); the
report newer than the last eval. `P12_MIN_SCREENS=8` is the only override you
may use without a note, and only if the clock forces it — say so in STATUS.)

When it exits 0, append `- <YYYY-MM-DD HH:MM>  GATE P12: PASS (<n> warnings)`
to `plan/STATUS.md` with the numbers named above on indented lines, commit,
push, **end your turn**.

## Rules (non-negotiable — these come from AGENTS.md)

- **Push, or it did not happen.** After every completed step: append one dated
  line to `plan/STATUS.md`, `git add`, `git commit`, `git pull --rebase`, `git
  push`. Never rewrite or reorder STATUS.md — append only. Another agent
  pushes to `main` concurrently, so always rebase before pushing.
- **Pod-state mutations go in the WORKLOG**, not git:
  `~/agents/2026-09-01_franka-sonic/WORKLOG.md`.
- Kit's portable-mode dirs must stay writable or every Isaac env creation dies:
  `sudo chown -R $USER:research /isaac-sim/kit/cache /isaac-sim/kit/logs /isaac-sim/kit/data /isaac-sim/extscache`
  (widen to `/isaac-sim/kit` if further `Permission denied` appears). Explicit
  exception to AGENTS.md rule (j); log it in the WORKLOG.
- **GPUs only from the allocator.** `harness/gpus.py acquire --n N --job <job>`,
  export what it prints, `release` when done. Never hand-pick a device; never a
  training rank and an Isaac eval on the same device (the probe "under load"
  in WP 12.3 shares a device with a *screen*, deliberately — the only
  exception, and the screen is launched first).
- **Job names must be unique across run roots.** `harness/gpus.py` keys claims
  by `<lane>-<stage>-<run folder>`; include the full run-folder name.
- **Long jobs**: `nohup <cmd> > <run>/logs/<name>.log 2>&1 & echo $! > <run>/out/<name>.pid`,
  then poll with **bounded foreground loops of ≤ 8 minutes per shell call**,
  repeating the call. Never a background wakeup, never an unbounded wait.
- **Kill only PIDs you recorded.** No `pkill -f`, no `killall`. Isaac trainers
  leave the Kit python alive after a wall-clock cap (`close()` hangs): SIGTERM
  the recorded pgid, then SIGKILL the python by pid, and verify with `ps`.
- **`/data/lustre/shared` is read-only.**
- **Never commit in upstream repos.** Canonical copies live in this repo under
  `harness/lane_a/` and `harness/lane_b/`.
- **Outputs only into run folders** under `~/runs/franka-sonic/<lane>/` or,
  where this prompt says so, `/tmp/franka-sonic/<lane>/` (instance-local, not
  persistent); code only in this repo. Nothing else in `/tmp`, nothing loose
  in `~`. Scratch for this phase: `/tmp/franka-sonic/p12/`.
- `harness/guard.sh` (PreToolUse hook) scans the whole command text, so a
  heredoc that merely *mentions* a forbidden word gets blocked. Workaround:
  write the script to a file in the run folder, then run the file.
- **Where this prompt or the plan leaves a detail open: decide it yourself,
  write the decision into STATUS.md, and continue.** Do not stop to ask.
- **If you are truly stuck** after real effort (not on the first error):
  append `BLOCKED: <one-line reason + what you tried>` to STATUS.md, push, and
  end your turn.
- **Never start the next phase**, whatever the state of the clock.
- A driver kills this session at **8 h** and relaunches up to three attempts.
  P12 spans several attempts by design (the fine-tune alone is ~9.5 h): write
  a RESUME POINTER (pids from `ps`, run folders, what is done and what is
  running) into STATUS.md at every milestone so the next attempt **resumes** —
  a live fine-tune or row is never restarted; `ps -p <pid>` first, always.

## Subagents

Use the `Agent` tool (`.claude/agents/executor.md`) for parallelisable work.

- **Only the parent acquires GPUs.** Hand the subagent the already-exported
  `CUDA_VISIBLE_DEVICES` set; never let it call `gpus.py acquire` for the same
  job, and at most one GPU-heavy job per allocated device set.
- **Foreground bounded waits only** — background wakeups fail silently in
  subagents.
- **No `git push` from a subagent.** It reports back; you commit, append to
  STATUS.md and push.
