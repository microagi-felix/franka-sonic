# STATUS — FR3 handover bake-off

Live handoff log. **Append one dated line after every completed step**, then
`git commit && git push`. The orchestrator on the Mac reads only this file and
the tmux pane. Gate verdicts go in as `GATE PN: PASS|FAIL`; a stop condition
goes in as `BLOCKED: <reason>`.

Format: `- YYYY-MM-DD HH:MM  <what happened>  [run=<path>] [rc=<n>]`

## Log

- 2026-09-03  P0 not started; repo scaffolded by orchestrator
- 2026-09-02 23:30  P0 env/bootstrap.sh completes cleanly and is idempotent: 3 consecutive runs exit 0, runs 2-3 all-skip with no new WORKLOG lines. Fixes committed: step 9 installs gear_sonic[training] through python.sh (pip then sees the prebundled torch 2.7.0/scipy/transformers, no second torch on Lustre; pyuser-sonic = 1.1 GB) and fetches the smpl_sim git+https dep over ssh via a GIT_CONFIG_COUNT rewrite scoped to that one pip call (git https to github.com fails for every repo on this pod, ssh works); step 5 idempotency now a pinned-version metadata check (bare Kit python has no numpy, pynput needs X, avp-stream is --no-deps). gear_sonic+smpl_sim+trl+accelerate+mujoco import under PYTHONUSERBASE=~/env/pyuser-sonic.  rc=0
- 2026-09-02 23:32  bakeoff shared/p0.smoke FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-02_p0.smoke
- 2026-09-02 23:43  bakeoff shared/p0.smoke OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-02_p0.smoke-2
- 2026-09-02 23:45  P0 p0.smoke OK on attempt 2. Attempt 1 died in gym.make (replicator annotator TypeError NoneType-NoneType) because Kit runs in portable mode rooted at /isaac-sim/kit and that root was not writable (kit/cache root-owned, kit/data + kit/logs absent -> no DerivedDataCache, no RTX shader db, no user.config.json -> renderer never came up). Fixed per orchestrator authorisation (rule-j exception, Felix 2026-09-02): bootstrap step 2 gives kit/cache, kit/logs, kit/data and extscache to $USER:research (idempotent, WORKLOG entry, overlay-only so it recurs per fresh pod). bakeoff.py fixes: config.json stamp no longer serialises the argparse callback (crashed attempt 0 before launch); stub server is stopped through Popen.wait (the kill-0 probe saw the unreaped zombie and reported "survived SIGKILL"). Attempt 2: stub server + evaluation.eval --client Cosmos --embodiment franka_dual --rate 15 --replan-every 8 --rollouts 1 --max-steps 100 --no-splat --headless -> episode len=100 success=False (hold policy, expected), eval_results.csv + episode_0.mp4 + run_summary.json written, stub stopped by SIGTERM, GPU 5 released. Remaining Permission-denied lines are carb.launcher exec of /usr/bin/nvidia-smi (absent on the pod, harmless). Kit-root relocation probe (--portable-root) verified working but dropped on orchestrator instruction; probe dir removed.  run=~/runs/franka-sonic/shared/2026-09-02_p0.smoke-2 rc=0
- 2026-09-02 23:45  GATE P0: PASS (0 warnings) — 10/10 checks PASS: isaac-sim group; /isaac-sim/python.sh; sim stack imports; evaluation.eval --help (58 flags); p0.smoke eval_results.csv (2 lines); gr00t import; GR00T-N1.7-3B cached (6.5G); gear_sonic import; tmux 3.4; gpus.py probe (2 eligible devices: 5, 7). P1 is unblocked but NOT started (stop condition per brief). Open for the orchestrator: (1) only 2 of 8 GPUs are allocatable (foreign memory on 0-4 and 6), so the parallel-lane schedule needs infra; (2) harness/gates/p0.sh covers the environment half of P0 only — the demo-set items in PLAN.md (scripted_source_demos -> MimicGen -> LeRobot/GR00T conversion -> replay gate) remain unchecked and have no gate script yet.
- 2026-09-02 23:51  DRIVER: start (pid 16036, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-02)
- 2026-09-02 23:55  ACK orchestrator rule (Felix): NEVER DELETE anything on this pod; nothing written, moved, chmod'ed or chown'ed outside ~ except the already-authorised /isaac-sim/kit cache, logs, data (+ extscache) ownership; conservative by default, BLOCKED on doubt. Acknowledged in the WORKLOG at 23:54Z together with a disclosure of what this session deleted BEFORE the rule arrived: the empty run folder left by the crashed smoke attempt 0 (zero files), the 4.7 MB ~/env/kit-portable probe dir, and scratch files under /tmp. Nothing outside ~ or /tmp, nothing belonging to another user.
- 2026-09-02 23:55  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-02_p0.smoke (failed smoke attempt 1: 88 KB of logs, no eval_results.csv; superseded by 2026-09-02_p0.smoke-2; kept in place per the no-delete rule).
- 2026-09-02 23:55  P0 interactive session: nothing left in P0 that this window may run. The environment half passed its gate at 23:45. The demo half (scripted_source_demos --rate 50 -> MimicGen -> export -> replay gate) is now owned by the driver's P1 session (attempt 1 started 23:51:41, P1 prompt WP 1.0 "check FIRST; collect if no HDF5 exists"), and AGENTS.md rule m forbids running the same work by hand next to it: with 2 allocatable devices the GPU claims and the git index would collide. Standing down; NOT blocked. If the orchestrator wants the demo half done in this window instead, stop the driver first (tmux kill-window -t bakeoff:driver) and say so.
- 2026-09-03 00:06  DRIVER: restarted by orchestrator to load Felix's no-delete / shared-folder / storage rules (AGENTS.md rule o, guard.py, HARD RULES block in every phase prompt, bakeoff.py /tmp fallback). P1 attempt 1 was aborted at ~13 min by PID (claude -p 16099, timeout 16098, driver 16036/16033); its orphaned children were stopped by their recorded PIDs (scripted_source_demos 19205/19212 and 18203, GPU holder 18113) and the stale gpus.json claim p1-demos was reaped. Nothing was deleted.
- 2026-09-03 00:06  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-03_demos — partial MimicGen source run from that aborted attempt (sources.hdf5 690 KB, incomplete; logs/apt_ffmpeg.log, sources.log). Kept in place per rule o; the new P1 attempt must start a fresh run folder, not resume this one.
- 2026-09-03 00:06  NEEDS-CLEANUP: /tmp/guard_battery.sh — guard-hook test battery copied in by the orchestrator to verify harness/guard.py on the pod copy. Harmless, kept per rule o.
- 2026-09-03 00:06  DRIVER: start (pid 22109, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-03)
- 2026-09-03 00:07  DRIVER: start (pid 22236, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-03)
- 2026-09-03 00:24  bakeoff shared/demos OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:31  P1 attempt 2 (driver pid 22236) WP 1.0 sources: 10/10 scripted source demos at 50 Hz saved on attempts 1-10 (lengths 483-806 steps = 10-16 s). Fix over the aborted attempt (3/6): harness/data/scripted_source_demos.py now scales the goto/grip/settle step budgets with rate/30 (400/33/25 at 50 Hz) — at 50 Hz an env step is 2 physics substeps, so the 30 Hz budgets gave the right arm 1.67x less simulated time and its hover stalled at dist 0.035 > tol 0.02. Mirrored right-arm grasp from attempt 1 kept. Isaac Lab's annotate/generate scripts are NOT in the franka repo (`.isaaclab/` is a gitignored clone this pod lacks) but exist at /workspace/isaaclab (2.3.2): harness/data/{annotate_sources,generate_handover,generate_parallel}.py are copies of the upstream wrappers that read ISAACLAB_ROOT and step the Mimic env at MIMIC_RATE_HZ=50 (the cfg hard-codes 30 Hz); nothing in the upstream repo was edited. harness/bakeoff.py gained --resume/--steps (sub-step .done markers) and the shared/demos stage; annotate->generate(80, 4 procs)->export(4 shards, video-backed half-res, IK joint targets)->replay now running as bakeoff pid 27095 on GPU 5.  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:32  bakeoff shared/demos FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:40  WP 1.0 annotate OK (10/10 sources auto-annotated at 50 Hz, ~2 min) but MimicGen died in all 4 workers: "subtask termination signal is not increasing: 255 should be greater than 255". Cause: on the 0.20 m angled rig the start slot is exactly 0.10 m from the centre pad, so mimic/mdp.py object_placed_in_zone (radius 0.10) is already TRUE at frame 0 and Isaac Lab Mimic takes its first non-zero diff — the falling edge when the left gripper closes, the same frame grasp_a rises — as the subtask end. Fix (harness/data/fix_subtask_signals.py, no upstream edit): copy the annotated file and zero left_placed before grasp_a's first rising edge; MimicGen boundaries verified in the copy. Relaunched fixsignals,generate,export,replay (bakeoff pipeline2).  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:40  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-03_demos-2/out/generated.hdf5 + generated_w{0..3}.hdf5 + generated_w{0..3}.log — empty outputs of the crashed MimicGen attempt (0 episodes); the retry writes generated-2.hdf5 (path recorded in out/generated.path). Kept per rule o.
- 2026-09-03 00:45  FINDING (repo): .gitignore had unanchored `runs`/`data` patterns for the two root symlinks, which also matched harness/data/ — every script there (scripted_source_demos, annotate/generate wrappers, export, fix_subtask_signals, converter) had been silently ignored by git; commit cf0f3d6's message named files it did not contain. Fixed in b9c085d (anchored /runs, /data; harness/data now tracked, 22 files under harness/). Subagent-authored (CPU-tested): harness/lane_a/modality_config_dual_fr3.py, harness/data/convert_hdf5_to_gr00t_v2.py (HDF5 -> GR00T v2.1 layout + gr00t stats + loader validation; lerobot is not on the pod so NVIDIA's v3->v2 script is unusable — route recorded here), harness/lane_a/serve_gr00t_joint.py (ZmqAct wire, 40-step chunk, replan 20, half-res frames). Decision: dataset state/action column order is [Lq7, Rq7, Lg, Rg] (prompt WP 1.1 split: joint_pos_l 0:7, joint_pos_r 7:14, grip 14:16); the wire order [Lq7, Lg, Rq7, Rg] is converted inside the server. NEEDS-CLEANUP: /tmp/franka-sonic/scratch_lane_a/ (subagent CPU test fixtures, ~5 MB, instance-local).
- 2026-09-03 01:31  bakeoff shared/demos FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 01:40  WP 1.0 generate OK: MimicGen 80/80 episodes at 50 Hz (4 single-env workers, 56.7 min, lengths 503-826 steps, mean 667; merged out/generated-2.hdf5). Keep-rate NOT recorded: Isaac Lab's generate_dataset prints its S/T tally to a block-buffered stdout that Kit's os._exit never flushes, so the worker logs end before it (keep_rate.json says so); the target >= 50 % is unverified — ~2.4 min per accepted episode per worker suggests roughly 1-in-2 to 1-in-3 trials succeeded. Export (4 shards, video-backed, half-res 640x360/424x240, IK joint targets + measured joints) running.
- 2026-09-03 01:40  DECISION lane A arm label = ik_target_clamped (harness/data/joint_labels.py), measured on one generated episode replayed through the JointPos env via harness/lane_a/eval_oracle_a.py on GPU 7: raw differential-IK joint command -> 6/6 milestones, success at frame 415, but the command jumps up to ~10 rad beyond the FR3 joint limits at segment starts (7 % of frames > 0.5 rad); clipped to the joint limits -> identical replay (6/6, frame 415), sane action range; per-step move clipped to 0.3 rad -> 4/6 (right arm misses the grasp); next measured joint position q_{t+1} (DATASET_SCHEMA convention) -> 4/6 (PD lag). The oracle and the converter share the label function so the A-oracle replays exactly the dataset's action. Export test on 1 episode: 503 frames in 18 s, replay_success=True; converter + gr00t stats + LeRobotEpisodeLoader validation pass on the real schema.
- 2026-09-03 01:57  bakeoff shared/demos OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 02:00  WP 1.0 DONE: export 80/80 generated episodes in 4 shards (~22 min on GPU 5; video-backed h264 at 640x360/424x240, 50 fps; obs pre-step; measured joints + differential-IK joint targets + initial cube pose; 76/80 replay to handover_success at 50 Hz with cameras, the 4 failed replays are flagged replay_success=False and skipped downstream). Replay check: generated episode 0 (813 rows) through the JointPos env via eval_oracle_a.py (ik_target_clamped) -> success=True, 6/6 milestones, stable at frame 725 (out/replay_check/eval_results.csv). Dataset conversion (shared/2026-09-03_dataset, CPU, 76 episodes / 51027 frames) running; lane_a/oracle_a (20 rollouts, GPU) starting now.  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 01:59  bakeoff shared/dataset OK  rc=0  gpus=none  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_dataset
- 2026-09-03 02:02  WP 1.1 DONE: shared/2026-09-03_dataset/out/gr00t_v2 — 76 episodes / 51027 frames at 50 Hz (4 failed replays skipped), GR00T v2 (LeRobot v2.1) layout written directly from the export HDF5 by harness/data/convert_hdf5_to_gr00t_v2.py (route: HDF5 -> GR00T v2; NVIDIA's convert_v3_to_v2.py needs lerobot, which is not on the pod); meta/modality.json state/action joint_pos_l 0:7 | joint_pos_r 7:14 | grip 14:16, video top/wrist_left/wrist_right (h264 640x360 / 424x240), annotation.human.task_description; gr00t/data/stats.py stats + relative_stats; LeRobotEpisodeLoader validation of 5 episodes OK. 106 MB.  run=~/runs/franka-sonic/shared/2026-09-03_dataset
- 2026-09-03 02:05  bakeoff lane_a/oracle_a OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_oracle_a
- 2026-09-03 02:06  WP 1.7 DONE: A-oracle 20/20 success (progress 1.0 on every episode; episode lengths 415-725 steps, terminate-on-success) — recorded ik_target_clamped joint commands + binary gripper of export episodes 0-19 replayed on their recorded cube spawns through evaluation.eval on the JointPos env variant (Isaac-Stack-Cube-DualFranka-JointPos-OracleA-v0, harness/lane_a/oracle_a_env.py), 50 Hz, horizon 1500, ~5 min on GPU 5. The dataset/replay path is sound; lane A's policy number is meaningful. WP 1.3 fine-tune launched 02:05 on GPUs 5,7 (torchrun 2 ranks, rev 3c hyperparameters).  run=~/runs/franka-sonic/lane_a/2026-09-03_oracle_a
- 2026-09-03 02:11  bakeoff lane_a/finetune FAILED  rc=1  gpus=5,7  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:16  WP 1.3 attempt 1 crashed at startup (02:11): torchrun 2 ranks -> HF Trainer + DeepSpeed ZeRO-2 -> `import deepspeed` -> installed_cuda_version() raises MissingCUDAException because this pod has NO CUDA toolkit (no /usr/local/cuda, no nvcc; DS_SKIP_CUDA_CHECK is consulted only after that probe). The pip wheel nvidia-cuda-nvcc-cu12 ships no nvcc binary (installed into the GR00T venv, unused, WORKLOG). Fix: harness/env/cuda_home_stub/bin/nvcc answers `nvcc -V` with the venv's CUDA 12.8; bakeoff's finetune stage exports CUDA_HOME to it. Nothing is compiled (adamw_torch, ZeRO-2 without fused/offload ops); `import deepspeed` verified. Dataset sharding had already succeeded (47 shards, 48063 steps/epoch). Relaunched 02:16 on GPUs 5,7, same run folder (attempt-1 logs kept).  run=~/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:15  bakeoff lane_a/finetune FAILED  rc=1  gpus=5,7  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:20  WP 1.3 attempt 2 (02:14, CUDA_HOME stub) loaded the model then aborted at 02:15:25 with NCCL "CUDA error: an illegal memory access was encountered" on both ranks. Isolated with a 2-rank all_reduce/broadcast smoke on GPUs 5,7 (torchrun, 64 MB tensors): default transport FAILS, NCCL_P2P_DISABLE=1 FAILS (shared-memory transport also faults in this container), NCCL_P2P_DISABLE=1 NCCL_SHM_DISABLE=1 (socket transport) PASSES. bakeoff's finetune stage now sets both; attempt 3 launched 02:17 on GPUs 5,7 (same run folder). Expect slower steps (grad all-reduce over loopback sockets) — recorded as a pod constraint, not a recipe change.  run=~/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:18  bakeoff lane_a/finetune FAILED  rc=1  gpus=5,7  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:28  WP 1.3 attempt 3 (02:17, NCCL sockets) got into the training loop and died in the first batch: "stack expects each tensor to be equal size, but got [1, 3, 256, 455] and [1, 3, 256, 452]" — GR00T stacks all camera views into one tensor after its shortest-edge-256 resize, so the 640x360 top and 424x240 wrists (different aspect ratios) cannot mix; PLAN.md's "one resolution" clause. Fix: converter --video-size 640x360 rescales every mp4 to one size with ffmpeg (area filter, h264/yuv420p, frame count verified), the joint server gets --image-size 640x360 for live frames (same cv2 INTER_AREA target), bakeoff passes both. Re-converting into shared/2026-09-03_dataset-2 (CPU). NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-03_dataset/out/gr00t_v2 — mixed-resolution dataset (106 MB), superseded by dataset-2; kept per rule o.
- 2026-09-03 02:22  bakeoff shared/dataset OK  rc=0  gpus=none  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_dataset-2
- 2026-09-03 02:25  WP 1.1 (revised) DONE: shared/2026-09-03_dataset-2/out/gr00t_v2 — same 76 episodes / 51027 frames, every camera 640x360 (152 wrist mp4s rescaled 424x240 -> 640x360 with ffmpeg area filter; top copied), stats + loader validation OK. WP 1.3 attempt 4 launched 02:23 on GPUs 5,7 against dataset-2 (torchrun 2 ranks, CUDA_HOME stub, NCCL socket transport, rev 3c hyperparameters).  run=~/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:40  WP 1.3 attempt 4 is training: checkpoint-500 written by 02:37 (launched 02:23, incl. ~4 min model load + shard caching) -> <= 1.7 s/step over loopback-socket NCCL; checkpoint-2000 expected ~03:10. The run.log shows nothing yet because bakeoff's tee buffered 8 KB before flushing (fixed: flush per line, applies to later stages). Next: open-loop eval on 500/1000/1500/2000 and the ZmqAct eval (20 rollouts) as soon as the GPUs free up.  run=~/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 02:50  WP 1.3 rate: checkpoint-500 was written 02:34:41-02:35:01, i.e. ~0.9 s/step after the ~4 min load (socket NCCL is not the bottleneck); checkpoint-2000 expected ~03:00. DISK NOTE: each GR00T checkpoint is 34 GB (6.9 GB bf16 model + 27 GB DeepSpeed optimizer/scheduler state under global_stepN/), so the prescribed --save-steps 500 writes ~136 GB to ~/runs (free was 1028 GB, above the 300 GB HARD-RULE floor; AGENTS.md rule j's 20 GB threshold is exceeded by the prescribed recipe itself — recorded, not deleted). NEEDS-CLEANUP (after P4): global_step{500,1000,1500}/ optimizer states under lane_a/2026-09-03_finetune/out/checkpoints — 27 GB each, only checkpoint-2000 is needed for eval. A CPU preflight of serve_gr00t_joint.py on checkpoint-500 was not possible (the checkpoint pins FlashAttention-2, GPU only); the GPU eval stage is the first end-to-end run of the server.
- 2026-09-03 03:08  bakeoff lane_a/finetune OK  rc=0  gpus=5,7  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 03:09  WP 1.3 DONE: lane A fine-tune rc=0 — 2000 steps in 2627 s (0.76 steps/s, 24.4 samples/s on GPUs 5,7), train_loss 0.1287; checkpoints 500/1000/1500/2000 under out/checkpoints (34 GB each). Launched at 03:08: lane_a/2026-09-03_eval (serve_gr00t_joint.py on checkpoint-2000 + evaluation.eval --client ZmqAct, 20 rollouts, 50 Hz, horizon 1500; server and Isaac share ONE GPU so the open-loop eval can use the other — decision, 2 allocatable devices) and lane_a/2026-09-03_open_loop (open_loop_eval.py on 500/1000/1500/2000, traj 0-2, horizon 40, 600 steps).  run=~/runs/franka-sonic/lane_a/2026-09-03_finetune
- 2026-09-03 03:18  WP 1.5/1.6 running: serve_gr00t_joint.py loaded checkpoint-2000 in 114 s on GPU 5 (policy replan latency ~85 ms, 40-step joint chunk, replan every 20), evaluation.eval --client ZmqAct on the JointPos env on the same GPU; episode 0: len=1500 success=False progress=0.167 (left arm reaches the block, no lift). WP 1.4 running on GPU 7: checkpoint-500 open-loop MSE 0.0747 / MAE 0.127 rad (traj 0-2, horizon 40).  run=~/runs/franka-sonic/lane_a/2026-09-03_eval
- 2026-09-03 03:19  bakeoff lane_a/open_loop OK  rc=0  gpus=7  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_open_loop
- 2026-09-03 03:28  WP 1.4 DONE: open-loop eval (traj 0-2, horizon 40, 600 steps) MSE / MAE (rad): ckpt-500 0.0747 / 0.127, ckpt-1000 0.1023 / 0.0956, ckpt-1500 0.1079 / 0.0812, ckpt-2000 0.0536 / 0.0601 — MAE falls monotonically, MSE is noisy (traj 2 dominates: 0.127 at 2000 vs 0.016/0.018 for traj 0/1); the fine-tune learned the data. out/open_loop_eval.json was re-parsed per checkpoint section (the stage had stored the first log match for every checkpoint; bakeoff fixed). Closed-loop so far (11/20 episodes): 0 successes, progress 0.167 / 0.5 / 0.167 then 0.0 x8 — the policy rarely even reaches the block.  run=~/runs/franka-sonic/lane_a/2026-09-03_open_loop
- 2026-09-03 03:33  ORCHESTRATOR: storage floor raised 300 -> 600 GB (home free 1200 -> 717 GB in 3.5 h, ~140 GB of it ours); new run folders go to /tmp/franka-sonic (instance-local, not persistent) below the floor; gates search both roots; finals copied back to ~/runs/franka-sonic/<lane>/final/.
- 2026-09-03 03:35  bakeoff lane_a/eval OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_eval
- 2026-09-03 03:36  ORCHESTRATOR: gate fix — p1.sh check 1 used `grep -vi sonic`, which drops every path under ~/runs/franka-sonic (could never PASS); p3.sh check 1 used `grep -iE sonic|token`, which matched every path (lane A dataset would false-PASS). Both now discriminate on /lane_b/, _sonic, token. P2 prompt: "delete failed-run artifacts" wording removed (rule o).
- 2026-09-03 03:36  NEEDS-CLEANUP: /tmp/p1_old_5ca3ed8.sh /tmp/p2_old_5ca3ed8.sh /tmp/p3_old_5ca3ed8.sh /tmp/p4_old_5ca3ed8.sh — orchestrator executor scratch copies (~18 KB) used to diff gate behaviour before/after the storage change; kept per rule o.
- 2026-09-03 03:40  WP 1.6 DONE: lane A closed-loop eval — 0/20 success, progress mean 0.042 (best episode 3/6 milestones); milestone reach rates: left reaches 0.15, left lifts 0.05, placed at centre 0.05, right reaches/lifts/places 0.00; every episode ran the full 1500-step horizon (~1 min each, replan latency ~85 ms). Diagnosis (out/open_loop_ckpt2000.jpeg + episode videos): open-loop the policy tracks the demos closely (MAE 0.06 rad) EXCEPT in the first chunk (steps 0-40), where it reproduces the demos' saturated differential-IK commands (swings of 2-4 rad on joints 1/3/6/7 at the arm's first move); in closed loop those targets fling the arm into an off-distribution pose it never recovers from, so it rarely even reaches the block. The label is ik_target_clamped by construction (the only label that replays the demos: A-oracle 20/20); the smoother alternatives (next-state, delta-clamped) failed the oracle. Recommended follow-up, NOT run (session budget, and P2 needs the GPUs): an inference-side per-replan target-excursion clamp in serve_gr00t_joint.py (|target - q_measured| <= ~0.5 rad per 0.4 s replan) evaluated into a separate folder, or a retrain on q_{t+1} labels with a slower oracle expectation.  run=~/runs/franka-sonic/lane_a/2026-09-03_eval
- 2026-09-03 03:40  NEEDS-CLEANUP: /tmp/franka-sonic/scratch_lane_a/ (CPU/GPU preflight fixtures, frames, oracle tests, ~60 MB, instance-local); ~/runs/franka-sonic/shared/2026-09-03_dataset (mixed-resolution first conversion, 106 MB); ~/runs/franka-sonic/shared/2026-09-03_demos (aborted attempt-1 sources); lane_a/2026-09-03_finetune/out/checkpoints/checkpoint-{500,1000,1500}/global_step*/ (DeepSpeed optimizer states, 27 GB each). All kept per rule o.
- 2026-09-03 03:41  GATE P1: PASS (0 warnings)
    dataset:   ~/runs/franka-sonic/shared/2026-09-03_dataset-2/out/gr00t_v2 (76 episodes, 51027 frames, 50 Hz, 3 cams 640x360, state/action [Lq7 | Rq7 | Lg Rg], label ik_target_clamped); demos: ~/runs/franka-sonic/shared/2026-09-03_demos-2 (10 sources, 80 MimicGen, 76 replay to success; keep-rate tally lost, see 01:40)
    fine-tune: ~/runs/franka-sonic/lane_a/2026-09-03_finetune/out/checkpoints/checkpoint-2000 (2000 steps, 2 GPUs, 44 min, train_loss 0.129)
    open-loop: ~/runs/franka-sonic/lane_a/2026-09-03_open_loop — MAE 0.127 / 0.096 / 0.081 / 0.060 rad at 500/1000/1500/2000 (MSE 0.075 / 0.102 / 0.108 / 0.054)
    lane A eval: ~/runs/franka-sonic/lane_a/2026-09-03_eval/out/eval/eval_results.csv — success 0/20, progress mean 0.042, milestones [0.15, 0.05, 0.05, 0, 0, 0]
    A-oracle:  ~/runs/franka-sonic/lane_a/2026-09-03_oracle_a/out/eval/eval_results.csv — success 20/20, progress 1.0, mean length 629 steps
    pod constraints found: no CUDA toolkit (DeepSpeed needs the CUDA_HOME stub), NCCL P2P+SHM fault between GPUs (socket transport), GR00T needs one resolution across camera views, only 2 of 8 GPUs allocatable
- 2026-09-03 04:18  P2 WP 2.1 OK: `import gear_sonic` works (editable install -> ~/GR00T-WholeBodyControl, Isaac Lab = /workspace/isaaclab 0.54.2); one gap fixed: gear_sonic.utils.motion_lib.torch_humanoid_batch imports open3d at module level and the bootstrap install lacked it -> pip --user open3d-cpu 0.19 into ~/env/pyuser-sonic (WORKLOG). Clones (WORKLOG): mujoco_menagerie @e4049d0 sparse franka_fr3 -> ~/code/upstream/mujoco_menagerie; GMR @bb1bbe4 -> ~/code/upstream/GMR. SMPL-X body models: `find ~ /data/lustre/shared -maxdepth 6 -iname 'SMPLX_*'` -> none; bones-seed's SMPL side is a 7-part UNEXTRACTED tar under read-only /data. DECISION: demo-only motion library (P0 demos + mirrors + augmentations); consequence: the token space covers tabletop handover reaching only (risk 2). GMR registration is still being authored as a dormant path (subagent) so the human-motion half is one body-model download away.
- 2026-09-03 04:18  P2 WP 2.2 DONE: harness/lane_b/dual_fr3.xml (generated by harness/lane_b/make_dual_fr3_xml.py from menagerie fr3.xml): one fixed `base` root + left_/right_ prefixed arms at the angled rig (frankas_assets/rig.py: +-0.10 m x, +0.005 m y, 0.14 m z, quat Rz(-90)Rx(-/+45)), 14 hinge DoF in document order left_fr3_joint1..7, right_fr3_joint1..7, 17 bodies, named meshes + explicit normalised quats (Humanoid_Batch parses attributes literally), inertials on base/link0 for the Isaac importer. Installed into ~/GR00T-WholeBodyControl/gear_sonic/data/assets/robot_description/mjcf/ with a `dual_fr3_assets` symlink to the menagerie meshes (harness/lane_b/install_gear_sonic.sh, WORKLOG). DECISION joint-6 convention: SONIC q6 = FR3 q6 - 2.5307 rad (link6 frame pre-rotated by Rz, range [-1.986, 1.986]) because the demos drive the left joint 6 to 4.517 rad (> pi) and the motion library's axis-angle->quaternion->axis-angle round trip wraps anything beyond pi by 2*pi; every other FR3 joint is inside (-pi, pi]. Applies at every FR3<->SONIC boundary (demo->pkl converter now; P3 labeller/decoder server later). Verified by harness/lane_b/check_dual_fr3_mjcf.py: Humanoid_Batch FK vs mujoco mj_forward 1.1e-7 m over random poses, dof round trip 1.2e-7 rad.
- 2026-09-03 04:18  P2 WP 2.5 DONE (subagent, CPU): motion library 1216 pkl / 52 MB at ~/runs/franka-sonic/lane_b/2026-09-03_motion_lib/out/motions (harness/lane_b/demos_to_sonic_pkl.py): 76 exported demos (measured joints, J6 offset applied, 50->30 fps) + `_M` mirrors + augmentations tw080/tw125/off1-3/noise1/crop1 and their mirrors; pkl = root_trans_offset zeros, pose_aa (T,17,3) MuJoCo body order, dof (T,14) MuJoCo joint order, root_rot wxyz identity (asserted), smpl_joints zeros, fps 30. Mirror rule found and verified numerically: swap arms and negate joints 1,3,5,7 (body-position error 1e-16; joint 7 pinned via a constant frame-relabel test). Checks: Humanoid_Batch FK vs mujoco 1.6e-7 m, dof_pos round trip 2.4e-7 rad, 0 NaN / 0 limit violations over all 1216 clips, MotionLibRobot loads on CPU (harness/lane_b/check_motion_lib_loads.py). Bonus validator: the demos' obs/eef_* are per-arm-base-frame TCP poses; MJCF FK with the shifted q6 + [0,0,0.1034] TCP offset reproduces them to 0.0 m.
- 2026-09-03 04:18  NEEDS-CLEANUP: /tmp/franka-sonic/lane_b/smoke, /tmp/franka-sonic/lane_b/smoke2 (2-demo converter smoke builds, 32 pkl each), /tmp/franka-sonic/lane_b/probe_*.py + scan_library.py (one-off probes) — instance-local, not persistent; ~/GR00T-WholeBodyControl/gear_sonic/data/assets/robot_description/usd/dual_fr3/ — EMPTY 492-byte USD layers left by the Isaac Sim 5.1 MJCF importer (see next line). Kept per rule o.
- 2026-09-03 04:18  P2 WP 2.3/2.4 in progress: harness/lane_b/robots_dual_fr3.py (14 DoF, 2x7 actuator groups: effort 87/12 Nm as frankas_assets/fr3.py, PD 400/80 + 200/20 = the franka repo's HIGH_PD tier, index maps derived from the two name lists, action scale 0.25 rad/unit for all joints — SONIC's 0.25*effort/stiffness formula would give 0.054/0.015 and could not reach the demos' excursions at action_clip 20) and harness/lane_b/sonic_dual_fr3.yaml (anchor `base`, vr_3point [left/right link7, base], reward points = the two flanges, 7 tracked bodies, feet_acc/undesired_contacts/foot_pos_xyz/push_robot nulled, root reset ranges zero, encoder_sample_probs g1 1.0 / teleop 0 / smpl 0 with zero aux-loss weight — the teleop/smpl encoders stay in the module because eval_agent_trl.py's ONNX export enumerates all three) are installed (robot_mapping + Dual_FR3Converter patched in place, WORKLOG). BLOCKER FOUND AND ROUTED AROUND: Isaac Lab's MjcfFileCfg needs the `isaacsim.asset.importer.mjcf` extension, which the headless Kit experience does not enable; enabled by hand, the Isaac Sim 5.1 importer first failed with "Used null prim" (relative dest path) and then wrote empty 492-byte USD layers and hung. Switching the Isaac side to a URDF generated from the same MJCF (harness/lane_b/make_dual_fr3_urdf.py, UrdfFileCfg like G1/H2).
- 2026-09-03 04:41  bakeoff lane_b/sonic_rl FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/lane_b/2026-09-03_sonic_rl
- 2026-09-03 04:45  bakeoff lane_b/sonic_rl OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/lane_b/2026-09-03_sonic_rl-2
- 2026-09-03 05:02  P2 WP 2.3/2.4 DONE: Isaac side spawns harness/lane_b/dual_fr3.urdf (generated from the MJCF by harness/lane_b/make_dual_fr3_urdf.py, same links/joints/poses incl. the joint-6 shift, menagerie collision STLs as meshes) through UrdfFileCfg like G1/H2 — the Isaac Sim 5.1 MJCF importer is unusable here (not enabled by the headless Kit experience; enabled by hand it wrote empty USD layers). Probe (harness/lane_b/probe_isaac_names.py, logs in lane_b/2026-09-03_sonic_smoke/logs): PhysX enumerates bodies/joints breadth-first (base, left_link0, right_link0, left_link1, ...; left_joint1, right_joint1, ...) = the lists in robots_dual_fr3.py (ORDER CHECK OK), fixed base, 17 bodies / 14 joints, flanges at reset = MuJoCo home FK to 1e-4 m, gains 400/80 + 200/20 applied, holding the ready pose sags <= 0.034 rad. Two findings on the way: (1) the URDF importer turns URDF joint `friction` into a PhysX friction COEFFICIENT, so menagerie's frictionloss 1.137 N*m became 1.137 x constraint force and glued the joints — URDF dynamics are now 0/0 (damping from the implicit PD); (2) simulation_app.close() never returns headless on this pod — probes hard-exit like gear_sonic does. Self-collision is OFF (decision: convex hulls interpenetrate at the folded ready pose; demos are collision-free by construction; inter-arm contact is not modelled). num_envs=1 smoke via bakeoff (lane_b/2026-09-03_sonic_rl-2, `harness/bakeoff.py run lane_b sonic_rl --tiny`): 3 PPO iterations, 1216 motions loaded, tokenizer 64-D (2 tokens x 32 FSQ), rc=0. Missing training deps found by the smoke and installed into ~/env/pyuser-sonic (WORKLOG): vector-quantize-pytorch 1.31.4 (+einx). wandb: gear_sonic's opt/wandb default is use_wandb=True online with the stored ~/.netrc credentials — the first smoke created one online run (id k19ytfo5, 0 steps); bakeoff now passes use_wandb=false and WANDB_MODE=offline.
- 2026-09-03 05:02  NEEDS-CLEANUP: ~/runs/franka-sonic/lane_b/2026-09-03_sonic_rl (first smoke attempt, died on the missing vector_quantize_pytorch import, 40 KB of logs); ~/GR00T-WholeBodyControl/gear_sonic/data/assets/robot_description/usd/dual_fr3/ (empty layers from the MJCF importer); /tmp/franka-sonic/lane_b/gmr_probe (GMR probe scratch, instance-local). Kept per rule o.
- 2026-09-03 05:02  P2 WP 2.5b (subagent, CPU): GMR @bb1bbe4 registered for dual_fr3 as a DORMANT path (no SMPL-X body models on the pod): harness/lane_b/gmr/{register_dual_fr3.py, smplx_to_dual_fr3.json, smpl_pkl_to_smplx_npz.py, README.md}; installed into ~/code/upstream/GMR working tree (params.py 4 dicts, choices list, ik config, --no-denylist flag in the batch script, plus a needed fix: mink.Configuration must start from the MJCF keyframe because FR3 joint 4's range excludes 0) — never committed there (WORKLOG). Verified without body models: constructor + IK solve on synthetic SMPL-X frames reach the flange targets to 0.5 mm. Blockers for the real thing, recorded in the README: SMPL-X models; GMR's save path hardcodes a floating base (7 of 14 joints would be dropped) so a fixed-base-aware driver + GMR-pkl -> SONIC-pkl converter are still needed; bones SMPL pkls are joblib, 50 fps, no betas.
