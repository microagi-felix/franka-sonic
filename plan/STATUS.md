# STATUS — FR3 handover bake-off

Live handoff log. **Append one dated line after every completed step**, then
`git commit && git push`. The orchestrator on the Mac reads only this file and
the tmux pane. Gate verdicts go in as `GATE PN: PASS|FAIL`; a stop condition
goes in as `BLOCKED: <reason>`.

Format: `- YYYY-MM-DD HH:MM  <what happened>  [run=<path>] [rc=<n>]`

## Log

- 2026-09-03  P0 not started; repo scaffolded by orchestrator
- 2026-09-02 23:30  P0 env/bootstrap.sh completes cleanly and is idempotent: 3 consecutive runs exit 0, runs 2-3 all-skip with no new WORKLOG lines. Fixes committed: step 9 installs gear_sonic[training] through python.sh (pip then sees the prebundled torch 2.7.0/scipy/transformers, no second torch on Lustre; pyuser-sonic = 1.1 GB) and fetches the smpl_sim git+https dep over ssh via a GIT_CONFIG_COUNT rewrite scoped to that one pip call (git https to github.com fails for every repo on this pod, ssh works); step 5 idempotency now a pinned-version metadata check (bare Kit python has no numpy, pynput needs X, avp-stream is --no-deps). gear_sonic+smpl_sim+trl+accelerate+mujoco import under PYTHONUSERBASE=~/env/pyuser-sonic.  rc=0
- 2026-09-02 23:32  bakeoff shared/p0.smoke FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-02_p0.smoke
- 2026-09-02 23:43  bakeoff shared/p0.smoke OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-02_p0.smoke-2
- 2026-09-02 23:45  P0 p0.smoke OK on attempt 2. Attempt 1 died in gym.make (replicator annotator TypeError NoneType-NoneType) because Kit runs in portable mode rooted at /isaac-sim/kit and that root was not writable (kit/cache root-owned, kit/data + kit/logs absent -> no DerivedDataCache, no RTX shader db, no user.config.json -> renderer never came up). Fixed per orchestrator authorisation (rule-j exception, Felix 2026-09-02): bootstrap step 2 gives kit/cache, kit/logs, kit/data and extscache to $USER:research (idempotent, WORKLOG entry, overlay-only so it recurs per fresh pod). bakeoff.py fixes: config.json stamp no longer serialises the argparse callback (crashed attempt 0 before launch); stub server is stopped through Popen.wait (the kill-0 probe saw the unreaped zombie and reported "survived SIGKILL"). Attempt 2: stub server + evaluation.eval --client Cosmos --embodiment franka_dual --rate 15 --replan-every 8 --rollouts 1 --max-steps 100 --no-splat --headless -> episode len=100 success=False (hold policy, expected), eval_results.csv + episode_0.mp4 + run_summary.json written, stub stopped by SIGTERM, GPU 5 released. Remaining Permission-denied lines are carb.launcher exec of /usr/bin/nvidia-smi (absent on the pod, harmless). Kit-root relocation probe (--portable-root) verified working but dropped on orchestrator instruction; probe dir removed.  run=~/runs/franka-sonic/shared/2026-09-02_p0.smoke-2 rc=0
- 2026-09-02 23:45  GATE P0: PASS (0 warnings) — 10/10 checks PASS: isaac-sim group; /isaac-sim/python.sh; sim stack imports; evaluation.eval --help (58 flags); p0.smoke eval_results.csv (2 lines); gr00t import; GR00T-N1.7-3B cached (6.5G); gear_sonic import; tmux 3.4; gpus.py probe (2 eligible devices: 5, 7). P1 is unblocked but NOT started (stop condition per brief). Open for the orchestrator: (1) only 2 of 8 GPUs are allocatable (foreign memory on 0-4 and 6), so the parallel-lane schedule needs infra; (2) harness/gates/p0.sh covers the environment half of P0 only — the demo-set items in PLAN.md (scripted_source_demos -> MimicGen -> LeRobot/GR00T conversion -> replay gate) remain unchecked and have no gate script yet.
- 2026-09-02 23:51  DRIVER: start (pid 16036, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-02)
- 2026-09-02 23:55  ACK orchestrator rule (Felix): NEVER DELETE anything on this pod; nothing written, moved, chmod'ed or chown'ed outside ~ except the already-authorised /isaac-sim/kit cache, logs, data (+ extscache) ownership; conservative by default, BLOCKED on doubt. Acknowledged in the WORKLOG at 23:54Z together with a disclosure of what this session deleted BEFORE the rule arrived: the empty run folder left by the crashed smoke attempt 0 (zero files), the 4.7 MB ~/env/kit-portable probe dir, and scratch files under /tmp. Nothing outside ~ or /tmp, nothing belonging to another user.
- 2026-09-02 23:55  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-02_p0.smoke (failed smoke attempt 1: 88 KB of logs, no eval_results.csv; superseded by 2026-09-02_p0.smoke-2; kept in place per the no-delete rule).
- 2026-09-02 23:55  P0 interactive session: nothing left in P0 that this window may run. The environment half passed its gate at 23:45. The demo half (scripted_source_demos --rate 50 -> MimicGen -> export -> replay gate) is now owned by the driver's P1 session (attempt 1 started 23:51:41, P1 prompt WP 1.0 "check FIRST; collect if no HDF5 exists"), and AGENTS.md rule m forbids running the same work by hand next to it: with 2 allocatable devices the GPU claims and the git index would collide. Standing down; NOT blocked. If the orchestrator wants the demo half done in this window instead, stop the driver first (tmux kill-window -t bakeoff:driver) and say so.
- 2026-09-03 00:06  DRIVER: restarted by orchestrator to load Felix's no-delete / shared-folder / storage rules (AGENTS.md rule o, guard.py, HARD RULES block in every phase prompt, bakeoff.py /tmp fallback). P1 attempt 1 was aborted at ~13 min by PID (claude -p 16099, timeout 16098, driver 16036/16033); its orphaned children were stopped by their recorded PIDs (scripted_source_demos 19205/19212 and 18203, GPU holder 18113) and the stale gpus.json claim p1-demos was reaped. Nothing was deleted.
- 2026-09-03 00:06  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-03_demos — partial MimicGen source run from that aborted attempt (sources.hdf5 690 KB, incomplete; logs/apt_ffmpeg.log, sources.log). Kept in place per rule o; the new P1 attempt must start a fresh run folder, not resume this one.
- 2026-09-03 00:06  NEEDS-CLEANUP: /tmp/guard_battery.sh — guard-hook test battery copied in by the orchestrator to verify harness/guard.py on the pod copy. Harmless, kept per rule o.
- 2026-09-03 00:06  DRIVER: start (pid 22109, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-03)
- 2026-09-03 00:07  DRIVER: start (pid 22236, effort xhigh, logs /home/felixminzenmay/runs/franka-sonic/driver/2026-09-03)
- 2026-09-03 00:24  bakeoff shared/demos OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:31  P1 attempt 2 (driver pid 22236) WP 1.0 sources: 10/10 scripted source demos at 50 Hz saved on attempts 1-10 (lengths 483-806 steps = 10-16 s). Fix over the aborted attempt (3/6): harness/data/scripted_source_demos.py now scales the goto/grip/settle step budgets with rate/30 (400/33/25 at 50 Hz) — at 50 Hz an env step is 2 physics substeps, so the 30 Hz budgets gave the right arm 1.67x less simulated time and its hover stalled at dist 0.035 > tol 0.02. Mirrored right-arm grasp from attempt 1 kept. Isaac Lab's annotate/generate scripts are NOT in the franka repo (`.isaaclab/` is a gitignored clone this pod lacks) but exist at /workspace/isaaclab (2.3.2): harness/data/{annotate_sources,generate_handover,generate_parallel}.py are copies of the upstream wrappers that read ISAACLAB_ROOT and step the Mimic env at MIMIC_RATE_HZ=50 (the cfg hard-codes 30 Hz); nothing in the upstream repo was edited. harness/bakeoff.py gained --resume/--steps (sub-step .done markers) and the shared/demos stage; annotate->generate(80, 4 procs)->export(4 shards, video-backed half-res, IK joint targets)->replay now running as bakeoff pid 27095 on GPU 5.  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:32  bakeoff shared/demos FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:40  WP 1.0 annotate OK (10/10 sources auto-annotated at 50 Hz, ~2 min) but MimicGen died in all 4 workers: "subtask termination signal is not increasing: 255 should be greater than 255". Cause: on the 0.20 m angled rig the start slot is exactly 0.10 m from the centre pad, so mimic/mdp.py object_placed_in_zone (radius 0.10) is already TRUE at frame 0 and Isaac Lab Mimic takes its first non-zero diff — the falling edge when the left gripper closes, the same frame grasp_a rises — as the subtask end. Fix (harness/data/fix_subtask_signals.py, no upstream edit): copy the annotated file and zero left_placed before grasp_a's first rising edge; MimicGen boundaries verified in the copy. Relaunched fixsignals,generate,export,replay (bakeoff pipeline2).  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 00:40  NEEDS-CLEANUP: ~/runs/franka-sonic/shared/2026-09-03_demos-2/out/generated.hdf5 + generated_w{0..3}.hdf5 + generated_w{0..3}.log — empty outputs of the crashed MimicGen attempt (0 episodes); the retry writes generated-2.hdf5 (path recorded in out/generated.path). Kept per rule o.
- 2026-09-03 00:45  FINDING (repo): .gitignore had unanchored `runs`/`data` patterns for the two root symlinks, which also matched harness/data/ — every script there (scripted_source_demos, annotate/generate wrappers, export, fix_subtask_signals, converter) had been silently ignored by git; commit cf0f3d6's message named files it did not contain. Fixed in b9c085d (anchored /runs, /data; harness/data now tracked, 22 files under harness/). Subagent-authored (CPU-tested): harness/lane_a/modality_config_dual_fr3.py, harness/data/convert_hdf5_to_gr00t_v2.py (HDF5 -> GR00T v2.1 layout + gr00t stats + loader validation; lerobot is not on the pod so NVIDIA's v3->v2 script is unusable — route recorded here), harness/lane_a/serve_gr00t_joint.py (ZmqAct wire, 40-step chunk, replan 20, half-res frames). Decision: dataset state/action column order is [Lq7, Rq7, Lg, Rg] (prompt WP 1.1 split: joint_pos_l 0:7, joint_pos_r 7:14, grip 14:16); the wire order [Lq7, Lg, Rq7, Rg] is converted inside the server. NEEDS-CLEANUP: /tmp/franka-sonic/scratch_lane_a/ (subagent CPU test fixtures, ~5 MB, instance-local).
- 2026-09-03 01:31  bakeoff shared/demos FAILED  rc=1  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 01:40  WP 1.0 generate OK: MimicGen 80/80 episodes at 50 Hz (4 single-env workers, 56.7 min, lengths 503-826 steps, mean 667; merged out/generated-2.hdf5). Keep-rate NOT recorded: Isaac Lab's generate_dataset prints its S/T tally to a block-buffered stdout that Kit's os._exit never flushes, so the worker logs end before it (keep_rate.json says so); the target >= 50 % is unverified — ~2.4 min per accepted episode per worker suggests roughly 1-in-2 to 1-in-3 trials succeeded. Export (4 shards, video-backed, half-res 640x360/424x240, IK joint targets + measured joints) running.
- 2026-09-03 01:40  DECISION lane A arm label = ik_target_clamped (harness/data/joint_labels.py), measured on one generated episode replayed through the JointPos env via harness/lane_a/eval_oracle_a.py on GPU 7: raw differential-IK joint command -> 6/6 milestones, success at frame 415, but the command jumps up to ~10 rad beyond the FR3 joint limits at segment starts (7 % of frames > 0.5 rad); clipped to the joint limits -> identical replay (6/6, frame 415), sane action range; per-step move clipped to 0.3 rad -> 4/6 (right arm misses the grasp); next measured joint position q_{t+1} (DATASET_SCHEMA convention) -> 4/6 (PD lag). The oracle and the converter share the label function so the A-oracle replays exactly the dataset's action. Export test on 1 episode: 503 frames in 18 s, replay_success=True; converter + gr00t stats + LeRobotEpisodeLoader validation pass on the real schema.
- 2026-09-03 01:57  bakeoff shared/demos OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 02:00  WP 1.0 DONE: export 80/80 generated episodes in 4 shards (~22 min on GPU 5; video-backed h264 at 640x360/424x240, 50 fps; obs pre-step; measured joints + differential-IK joint targets + initial cube pose; 76/80 replay to handover_success at 50 Hz with cameras, the 4 failed replays are flagged replay_success=False and skipped downstream). Replay check: generated episode 0 (813 rows) through the JointPos env via eval_oracle_a.py (ik_target_clamped) -> success=True, 6/6 milestones, stable at frame 725 (out/replay_check/eval_results.csv). Dataset conversion (shared/2026-09-03_dataset, CPU, 76 episodes / 51027 frames) running; lane_a/oracle_a (20 rollouts, GPU) starting now.  run=~/runs/franka-sonic/shared/2026-09-03_demos-2
- 2026-09-03 01:59  bakeoff shared/dataset OK  rc=0  gpus=none  run=/home/felixminzenmay/runs/franka-sonic/shared/2026-09-03_dataset
- 2026-09-03 02:02  WP 1.1 DONE: shared/2026-09-03_dataset/out/gr00t_v2 — 76 episodes / 51027 frames at 50 Hz (4 failed replays skipped), GR00T v2 (LeRobot v2.1) layout written directly from the export HDF5 by harness/data/convert_hdf5_to_gr00t_v2.py (route: HDF5 -> GR00T v2; NVIDIA's convert_v3_to_v2.py needs lerobot, which is not on the pod); meta/modality.json state/action joint_pos_l 0:7 | joint_pos_r 7:14 | grip 14:16, video top/wrist_left/wrist_right (h264 640x360 / 424x240), annotation.human.task_description; gr00t/data/stats.py stats + relative_stats; LeRobotEpisodeLoader validation of 5 episodes OK. 106 MB.  run=~/runs/franka-sonic/shared/2026-09-03_dataset
- 2026-09-03 02:05  bakeoff lane_a/oracle_a OK  rc=0  gpus=5  run=/home/felixminzenmay/runs/franka-sonic/lane_a/2026-09-03_oracle_a
- 2026-09-03 02:06  WP 1.7 DONE: A-oracle 20/20 success (progress 1.0 on every episode; episode lengths 415-725 steps, terminate-on-success) — recorded ik_target_clamped joint commands + binary gripper of export episodes 0-19 replayed on their recorded cube spawns through evaluation.eval on the JointPos env variant (Isaac-Stack-Cube-DualFranka-JointPos-OracleA-v0, harness/lane_a/oracle_a_env.py), 50 Hz, horizon 1500, ~5 min on GPU 5. The dataset/replay path is sound; lane A's policy number is meaningful. WP 1.3 fine-tune launched 02:05 on GPUs 5,7 (torchrun 2 ranks, rev 3c hyperparameters).  run=~/runs/franka-sonic/lane_a/2026-09-03_oracle_a
